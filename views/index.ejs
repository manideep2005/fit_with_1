<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Fit-With-AI - Creative Solutions</title>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/ScrollTrigger.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/TextPlugin.min.js"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
  <style>
            * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            background-color: #0a0a0a;
            color: white;
            overflow-x: hidden;
        }

        .container {
            width: 100%;
            min-height: 100vh;
            position: relative;
        }

        /* Header */
        header {
            padding: 1.5rem 5%;
            display: flex;
            justify-content: space-between;
            align-items: center;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            z-index: 10;
            background-color: rgba(10, 10, 10, 0.8);
            backdrop-filter: blur(10px);
        }

        .logo {
            font-size: 2rem;
            font-weight: 800;
            background: linear-gradient(90deg, #ff6b6b, #4ecdc4, #8a2be2);
            -webkit-background-clip: text;
            background-clip: text;
            -webkit-text-fill-color: transparent;
            letter-spacing: 2px;
        }

        .nav-links {
            display: flex;
            gap: 2rem;
        }

        .nav-links a {
            color: white;
            text-decoration: none;
            font-weight: 500;
            position: relative;
            padding: 0.5rem 0;
            transition: 0.3s ease;
        }

        .nav-links a:hover {
            color: #4ecdc4;
        }

        .nav-links a::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 0;
            height: 2px;
            background: linear-gradient(90deg, #ff6b6b, #4ecdc4);
            transition: width 0.3s ease;
        }

        .nav-links a:hover::after {
            width: 100%;
        }

        .auth-buttons {
            display: flex;
            gap: 1rem;
        }

        .auth-btn {
            padding: 0.7rem 1.5rem;
            border: none;
            border-radius: 30px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .signin-btn {
            background-color: transparent;
            color: white;
            border: 2px solid #4ecdc4;
        }

        .signin-btn:hover {
            background-color: rgba(78, 205, 196, 0.1);
            transform: translateY(-3px);
        }

        .signup-btn {
            background: linear-gradient(90deg, #ff6b6b, #4ecdc4);
            color: white;
        }

        .signup-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 20px rgba(78, 205, 196, 0.3);
        }

        /* Hero Section */
        .hero {
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            overflow: hidden;
        }

        .hero-bg {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
        }

        .hero-bg::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: radial-gradient(circle, rgba(0,0,0,0.5) 0%, rgba(0,0,0,0.9) 100%);
        }

        .hero-image {
            position: absolute;
            width: 100%;
            height: 100%;
            object-fit: cover;
            opacity: 0.4;
        }

        .hero-content {
            max-width: 800px;
            padding: 0 2rem;
            text-align: center;
            z-index: 1;
        }

        .hero-title {
            font-size: 4rem;
            margin-bottom: 1.5rem;
            line-height: 1.2;
        }

        .hero-title span {
            position: relative;
            display: inline-block;
        }

        .changing-text {
            color: #4ecdc4;
            position: relative;
        }

        .hero-subtitle {
            font-size: 1.2rem;
            margin-bottom: 2rem;
            opacity: 0.9;
            line-height: 1.6;
        }

        .hero-buttons {
            display: flex;
            gap: 1.5rem;
            justify-content: center;
        }

        .hero-btn {
            padding: 1rem 2rem;
            border: none;
            border-radius: 30px;
            font-weight: 600;
            font-size: 1rem;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .primary-btn {
            background: linear-gradient(90deg, #ff6b6b, #4ecdc4);
            color: white;
        }

        .primary-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 20px rgba(78, 205, 196, 0.3);
        }

        .secondary-btn {
            background-color: transparent;
            color: white;
            border: 2px solid white;
        }

        .secondary-btn:hover {
            background-color: rgba(255, 255, 255, 0.1);
            transform: translateY(-3px);
        }

        /* Features Section */
       /* Features Section Styles */
.features {
    padding: 100px 20px;
    background: linear-gradient(135deg, #0a0a0a 0%, #1a1a1a 50%, #2a2a2a 100%);
    position: relative;
    overflow: hidden;
}

.features::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: radial-gradient(
        ellipse at 20% 50%, 
        rgba(255, 107, 157, 0.1) 0%, 
        transparent 50%
    ),
    radial-gradient(
        ellipse at 80% 20%, 
        rgba(79, 195, 247, 0.1) 0%, 
        transparent 50%
    );
    pointer-events: none;
}

/* Section Title */
.features .section-title {
    text-align: center;
    font-size: 3.5rem;
    font-weight: 800;
    color: #ffffff;
    margin-bottom: 80px;
    position: relative;
    z-index: 2;
}

.features .section-title span {
    background: linear-gradient(90deg, #ff6b6b, #4ecdc4, #8a2be2);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    position: relative;
}


.features .section-title span::after {
    content: '';
    position: absolute;
    bottom: -15px;
    left: 50%;
    transform: translateX(-50%);
    width: 80px;
    height: 4px;
    background: linear-gradient(90deg, 
        #ff6b9d 0%, 
        #ff8a5c 25%, 
        #ffd93d 50%, 
        #6bcf7f 75%, 
        #4fc3f7 100%
    );
    border-radius: 2px;
}

/* Features Grid */
.features-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
    gap: 40px;
    max-width: 1400px;
    margin: 0 auto;
    position: relative;
    z-index: 2;
}

/* Feature Cards */
.feature-card {
    background: rgba(255, 255, 255, 0.05);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 24px;
    padding: 40px 30px;
    text-align: center;
    position: relative;
    overflow: hidden;
    transform: translateY(50px);
    opacity: 0;
    transition: all 0.6s cubic-bezier(0.4, 0, 0.2, 1);
    cursor: pointer;
}

.feature-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(135deg, 
        rgba(255, 107, 157, 0.1) 0%,
        rgba(255, 138, 92, 0.1) 25%,
        rgba(255, 217, 61, 0.1) 50%,
        rgba(107, 207, 127, 0.1) 75%,
        rgba(79, 195, 247, 0.1) 100%
    );
    opacity: 0;
    transition: opacity 0.4s ease;
    pointer-events: none;
}

.feature-card.stagger-animate {
    animation: slideUp 0.8s ease-out forwards;
}

.feature-card:nth-child(1) { animation-delay: 0.1s; }
.feature-card:nth-child(2) { animation-delay: 0.2s; }
.feature-card:nth-child(3) { animation-delay: 0.3s; }
.feature-card:nth-child(4) { animation-delay: 0.4s; }
.feature-card:nth-child(5) { animation-delay: 0.5s; }
.feature-card:nth-child(6) { animation-delay: 0.6s; }

/* Feature Icon */
.feature-icon {
    width: 80px;
    height: 80px;
    margin: 0 auto 30px;
    border-radius: 20px;
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
    background: linear-gradient(135deg, 
        rgba(255, 107, 157, 0.2) 0%,
        rgba(255, 138, 92, 0.2) 25%,
        rgba(255, 217, 61, 0.2) 50%,
        rgba(107, 207, 127, 0.2) 75%,
        rgba(79, 195, 247, 0.2) 100%
    );
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.2);
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
}

.feature-icon i {
    font-size: 2rem;
    background: linear-gradient(45deg, 
        #ff6b9d 0%, 
        #ff8a5c 25%, 
        #ffd93d 50%, 
        #6bcf7f 75%, 
        #4fc3f7 100%
    );
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    transition: transform 0.4s cubic-bezier(0.4, 0, 0.2, 1);
}

/* Feature Title */
.feature-title {
    font-size: 1.5rem;
    font-weight: 700;
    color: #ffffff;
    margin-bottom: 20px;
    transition: color 0.3s ease;
}

/* Feature Description */
.feature-desc {
    font-size: 1rem;
    color: rgba(255, 255, 255, 0.8);
    line-height: 1.7;
    transition: color 0.3s ease;
}

/* Hover Effects */
.feature-card:hover {
    transform: translateY(-15px) scale(1.02);
    border-color: rgba(255, 107, 157, 0.3);
    box-shadow: 0 25px 60px rgba(0, 0, 0, 0.3),
                0 0 40px rgba(255, 107, 157, 0.1);
}

.feature-card:hover::before {
    opacity: 1;
}

.feature-card:hover .feature-icon {
    transform: scale(1.1) rotateY(15deg);
    background: linear-gradient(135deg, 
        rgba(255, 107, 157, 0.4) 0%,
        rgba(255, 138, 92, 0.4) 25%,
        rgba(255, 217, 61, 0.4) 50%,
        rgba(107, 207, 127, 0.4) 75%,
        rgba(79, 195, 247, 0.4) 100%
    );
    box-shadow: 0 15px 30px rgba(255, 107, 157, 0.2);
}

.feature-card:hover .feature-icon i {
    transform: scale(1.1);
}

.feature-card:hover .feature-title {
    background: linear-gradient(45deg, 
        #ff6b9d 0%, 
        #ff8a5c 25%, 
        #ffd93d 50%, 
        #6bcf7f 75%, 
        #4fc3f7 100%
    );
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
}

.feature-card:hover .feature-desc {
    color: rgba(255, 255, 255, 0.95);
}

/* Animations */
@keyframes slideUp {
    to {
        transform: translateY(0);
        opacity: 1;
    }
}

/* Responsive Design */
@media (max-width: 768px) {
    .features {
        padding: 80px 15px;
    }
    
    .features .section-title {
        font-size: 2.5rem;
        margin-bottom: 60px;
    }
    
    .features-grid {
        grid-template-columns: 1fr;
        gap: 30px;
    }
    
    .feature-card {
        padding: 30px 25px;
    }
    
    .feature-icon {
        width: 70px;
        height: 70px;
        margin-bottom: 25px;
    }
    
    .feature-icon i {
        font-size: 1.8rem;
    }
    
    .feature-title {
        font-size: 1.3rem;
        margin-bottom: 15px;
    }
    
    .feature-desc {
        font-size: 0.95rem;
    }
}

@media (max-width: 480px) {
    .features .section-title {
        font-size: 2rem;
    }
    
    .feature-card {
        padding: 25px 20px;
    }
    
    .feature-icon {
        width: 60px;
        height: 60px;
    }
    
    .feature-icon i {
        font-size: 1.5rem;
    }
}

/* Special hover effects for different cards */
.feature-card:nth-child(1):hover {
    box-shadow: 0 25px 60px rgba(0, 0, 0, 0.3),
                0 0 40px rgba(255, 107, 157, 0.15);
}

.feature-card:nth-child(2):hover {
    box-shadow: 0 25px 60px rgba(0, 0, 0, 0.3),
                0 0 40px rgba(255, 138, 92, 0.15);
}

.feature-card:nth-child(3):hover {
    box-shadow: 0 25px 60px rgba(0, 0, 0, 0.3),
                0 0 40px rgba(255, 217, 61, 0.15);
}

.feature-card:nth-child(4):hover {
    box-shadow: 0 25px 60px rgba(0, 0, 0, 0.3),
                0 0 40px rgba(107, 207, 127, 0.15);
}

.feature-card:nth-child(5):hover {
    box-shadow: 0 25px 60px rgba(0, 0, 0, 0.3),
                0 0 40px rgba(79, 195, 247, 0.15);
}

.feature-card:nth-child(6):hover {
    box-shadow: 0 25px 60px rgba(0, 0, 0, 0.3),
                0 0 40px rgba(156, 92, 255, 0.15);
}
        /* About Section */
        .about {
            padding: 6rem 5%;
            background-color: #0a0a0a;
            position: relative;
            overflow: hidden;
        }

        .about-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 4rem;
            align-items: center;
        }

        .about-image-container {
            position: relative;
            border-radius: 15px;
            overflow: hidden;
            box-shadow: white;
        }

        .about-image {
            width: 100%;
            height: auto;
            display: block;
            transition: transform 0.5s ease;
        }

        .about-image-container:hover .about-image {
            transform: scale(1.05);
        }

        .about-content h2 {
            font-size: 2.5rem;
            margin-bottom: 1.5rem;
            color: white;
        }

        .about-content h2 span {
            color: #4ecdc4;
        }

        .about-content p {
            color: rgba(255, 255, 255, 0.8);
            margin-bottom: 1.5rem;
            line-height: 1.7;
            font-size: 1.1rem;
        }

        .stats {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 1.5rem;
            margin-top: 2.5rem;
        }

        .stat-item {
            text-align: center;
        }

        .stat-number {
            font-size: 2.5rem;
            font-weight: 700;
            color: #4ecdc4;
            margin-bottom: 0.5rem;
        }

        .stat-label {
            color: rgba(255, 255, 255, 0.7);
            font-size: 0.9rem;
        }

        /* Gallery Section */
      /* Gallery Section Styles */
.gallery {
    padding: 80px 20px;
    background: linear-gradient(135deg, #1a1a1a 0%, #2d2d2d 100%);
    min-height: 100vh;
    position: relative;
    overflow: hidden;
}

.gallery::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: radial-gradient(circle at 50% 50%, rgba(255, 107, 0, 0.1) 0%, transparent 70%);
    pointer-events: none;
}

.section-title {
    text-align: center;
    font-size: 3.5rem;
    font-weight: 700;
    color: #ffffff;
    margin-bottom: 60px;
    position: relative;
    z-index: 2;
}

.section-title span {
    background: linear-gradient(90deg, #ff6b6b, #4ecdc4, #8a2be2);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    position: relative;
}

.section-title span::after {
    content: '';
    position: absolute;
    bottom: -10px;
    left: 50%;
    transform: translateX(-50%);
    width: 60px;
    height: 4px;
    background: linear-gradient(45deg, #ff6b00, #ff8f00);
    border-radius: 2px;
}

.gallery-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 30px;
    max-width: 1400px;
    margin: 0 auto;
    position: relative;
    z-index: 2;
}

.gallery-item {
    position: relative;
    border-radius: 20px;
    overflow: hidden;
    aspect-ratio: 1;
    background: #333;
    cursor: pointer;
    transform: translateY(50px);
    opacity: 0;
    transition: all 0.6s cubic-bezier(0.4, 0, 0.2, 1);
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
}

.gallery-item.stagger-animate {
    animation: slideUp 0.6s ease-out forwards;
}

.gallery-item:nth-child(1) { animation-delay: 0.1s; }
.gallery-item:nth-child(2) { animation-delay: 0.2s; }
.gallery-item:nth-child(3) { animation-delay: 0.3s; }
.gallery-item:nth-child(4) { animation-delay: 0.4s; }
.gallery-item:nth-child(5) { animation-delay: 0.5s; }
.gallery-item:nth-child(6) { animation-delay: 0.6s; }
.gallery-item:nth-child(7) { animation-delay: 0.7s; }
.gallery-item:nth-child(8) { animation-delay: 0.8s; }

.gallery-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.6s cubic-bezier(0.4, 0, 0.2, 1);
}

.gallery-overlay {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(
        135deg,
        rgba(255, 107, 0, 0.9) 0%,
        rgba(255, 143, 0, 0.8) 100%
    );
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    text-align: center;
    padding: 30px;
    opacity: 0;
    transform: translateY(30px);
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
}

.gallery-title {
    font-size: 1.8rem;
    font-weight: 700;
    color: #ffffff;
    margin-bottom: 15px;
    transform: translateY(20px);
    transition: transform 0.4s cubic-bezier(0.4, 0, 0.2, 1) 0.1s;
}

.gallery-desc {
    font-size: 1rem;
    color: #ffffff;
    line-height: 1.6;
    opacity: 0.9;
    transform: translateY(20px);
    transition: transform 0.4s cubic-bezier(0.4, 0, 0.2, 1) 0.2s;
}

/* Hover Effects */
.gallery-item:hover {
    transform: translateY(-10px) scale(1.02);
    box-shadow: 0 20px 50px rgba(255, 107, 0, 0.3);
}

.gallery-item:hover .gallery-image {
    transform: scale(1.1);
}

.gallery-item:hover .gallery-overlay {
    opacity: 1;
    transform: translateY(0);
}

.gallery-item:hover .gallery-title,
.gallery-item:hover .gallery-desc {
    transform: translateY(0);
}

/* Animations */
@keyframes slideUp {
    to {
        transform: translateY(0);
        opacity: 1;
    }
}

/* Responsive Design */
@media (max-width: 768px) {
    .gallery {
        padding: 60px 15px;
    }
    
    .section-title {
        font-size: 2.5rem;
        margin-bottom: 40px;
    }
    
    .gallery-grid {
        grid-template-columns: 1fr;
        gap: 20px;
    }
    
    .gallery-item {
        aspect-ratio: 4/3;
    }
    
    .gallery-title {
        font-size: 1.5rem;
    }
    
    .gallery-desc {
        font-size: 0.9rem;
    }
}

@media (max-width: 480px) {
    .section-title {
        font-size: 2rem;
    }
    
    .gallery-overlay {
        padding: 20px;
    }
    
    .gallery-title {
        font-size: 1.3rem;
        margin-bottom: 10px;
    }
}

/* Additional Enhancement: Glowing border effect */
.gallery-item::before {
    content: '';
    position: absolute;
    top: -2px;
    left: -2px;
    right: -2px;
    bottom: -2px;
    background: linear-gradient(45deg, #ff6b00, #ff8f00, #ff6b00);
    border-radius: 22px;
    opacity: 0;
    z-index: -1;
    transition: opacity 0.3s ease;
}

.gallery-item:hover::before {
    opacity: 0.7;
}
        /* Auth Modal */
        .auth-modal {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.8);
            display: flex;
            justify-content: center;
            align-items: center;
            z-index: 100;
            opacity: 0;
            visibility: hidden;
            transition: all 0.3s ease;
        }

        .auth-modal.active {
            opacity: 1;
            visibility: visible;
        }

        .modal-container {
            width: 400px;
            background-color: #111;
            border-radius: 15px;
            box-shadow: 0 25px 50px rgba(0, 0, 0, 0.5);
            overflow: hidden;
            transform: translateY(-50px);
            transition: all 0.3s ease;
        }

        .auth-modal.active .modal-container {
            transform: translateY(0);
        }

        .modal-tabs {
            display: flex;
        }

        .modal-tab {
            flex: 1;
            padding: 1.5rem;
            text-align: center;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            background-color: #222;
            color: rgba(255, 255, 255, 0.7);
        }

        .modal-tab.active {
            background-color: #111;
            color: white;
        }

        .modal-content {
            padding: 2rem;
        }

        .auth-form {
            display: none;
        }

        .auth-form.active {
            display: block;
        }

        .form-group {
            margin-bottom: 1.5rem;
        }

        .form-label {
            display: block;
            margin-bottom: 0.5rem;
            color: rgba(255, 255, 255, 0.8);
            font-size: 0.9rem;
        }

        .form-input {
            width: 100%;
            padding: 1rem;
            background-color: rgba(255, 255, 255, 0.1);
            border: none;
            border-radius: 5px;
            color: white;
            font-size: 1rem;
            transition: all 0.3s ease;
        }

        .form-input:focus {
            outline: none;
            background-color: rgba(255, 255, 255, 0.15);
            box-shadow: 0 0 0 2px rgba(78, 205, 196, 0.3);
        }

        .form-btn {
            width: 100%;
            padding: 1rem;
            background: linear-gradient(90deg, #ff6b6b, #4ecdc4);
            border: none;
            border-radius: 5px;
            color: white;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .form-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.3);
        }

        .form-footer {
            margin-top: 1.5rem;
            text-align: center;
            color: rgba(255, 255, 255, 0.6);
            font-size: 0.9rem;
        }

        .form-footer a {
            color: #4ecdc4;
            text-decoration: none;
            transition: color 0.3s ease;
        }

        .form-footer a:hover {
            color: #ff6b6b;
        }

        .modal-close {
            position: absolute;
            top: 1rem;
            right: 1rem;
            color: white;
            background: none;
            border: none;
            font-size: 1.5rem;
            cursor: pointer;
            opacity: 0.7;
            transition: opacity 0.3s ease;
        }

        .modal-close:hover {
            opacity: 1;
        }

        /* Footer */
        footer {
            background-color: #0a0a0a;
            padding: 5rem 5% 2rem;
            position: relative;
        }

        .footer-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 3rem;
            margin-bottom: 3rem;
        }

        .footer-col h3 {
            color: white;
            font-size: 1.5rem;
            margin-bottom: 1.5rem;
            position: relative;
        }

        .footer-col h3::after {
            content: '';
            position: absolute;
            bottom: -10px;
            left: 0;
            width: 50px;
            height: 2px;
            background: linear-gradient(90deg, #ff6b6b, #4ecdc4);
        }

        .footer-col p {
            color: rgba(255, 255, 255, 0.7);
            line-height: 1.6;
            margin-bottom: 1.5rem;
        }

        .footer-col ul {
            list-style: none;
        }

        .footer-col ul li {
            margin-bottom: 0.8rem;
        }

        .footer-col ul li a {
            color: rgba(255, 255, 255, 0.7);
            text-decoration: none;
            transition: all 0.3s ease;
            display: inline-block;
        }

        .footer-col ul li a:hover {
            color: #4ecdc4;
            transform: translateX(5px);
        }

        .social-links {
            display: flex;
            gap: 1rem;
        }

        .social-link {
            width: 40px;
            height: 40px;
            display: flex;
            align-items: center;
            justify-content: center;
            background-color: rgba(255, 255, 255, 0.1);
            border-radius: 50%;
            color: white;
            text-decoration: none;
            transition: all 0.3s ease;
        }

        .social-link:hover {
            background-color: #4ecdc4;
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.3);
        }

        .footer-bottom {
            text-align: center;
            padding-top: 2rem;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
            color: rgba(255, 255, 255, 0.5);
            font-size: 0.9rem;
        }

        /* Parallax Effect */
        .parallax-bg {
            position: absolute;
            width: 100%;
            height: 100%;
            top: 0;
            left: 0;
            z-index: -1;
            overflow: hidden;
        }

        .parallax-circle {
            position: absolute;
            border-radius: 50%;
            opacity: 0.1;
        }

        .circle-1 {
            width: 500px;
            height: 500px;
            background: linear-gradient(135deg, #ff6b6b, #4ecdc4);
            bottom: -250px;
            right: -150px;
        }

        .circle-2 {
            width: 300px;
            height: 300px;
            background: linear-gradient(135deg, #8a2be2, #4ecdc4);
            top: -150px;
            left: -100px;
        }

        /* Responsive Design */
        @media (max-width: 992px) {
            .hero-title {
                font-size: 3rem;
            }

            .nav-links {
                display: none;
            }

            .about-grid {
                grid-template-columns: 1fr;
                gap: 3rem;
            }

            .about-image-container {
                order: -1;
            }
        }

        @media (max-width: 768px) {
            .hero-title {
                font-size: 2.5rem;
            }

            .hero-subtitle {
                font-size: 1rem;
            }

            .hero-buttons {
                flex-direction: column;
                gap: 1rem;
            }

            .feature-card {
                padding: 1.5rem;
            }

            .modal-container {
                width: 90%;
            }
        }

        /* Animation Classes */
        .fade-in {
            opacity: 0;
            transform: translateY(20px);
            transition: opacity 0.6s ease, transform 0.6s ease;
        }

        .fade-in.active {
            opacity: 1;
            transform: translateY(0);
        }

        .stagger-animate {
            opacity: 0;
            transform: translateY(30px);
        }

        /* Floating animation */
        @keyframes float {
            0% {
                transform: translateY(0px);
            }
            50% {
                transform: translateY(-20px);
            }
            100% {
                transform: translateY(0px);
            }
        }

        .float {
            animation: float 6s ease-in-out infinite;
        }

        /* Preloader */
        .preloader {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: #0a0a0a;
            display: flex;
            justify-content: center;
            align-items: center;
            z-index: 1000;
            transition: opacity 0.6s ease, visibility 0.6s ease;
        }

        .preloader.hidden {
            opacity: 0;
            visibility: hidden;
        }

        .loader {
            width: 80px;
            height: 80px;
            border: 4px solid #4ecdc4;
            border-radius: 50%;
            border-top-color: transparent;
            animation: spin 1s linear infinite;
        }

        @keyframes spin {
            100% {
                transform: rotate(360deg);
            }
        }
  </style>
</head>
<body>
    <!-- Preloader -->
    <div class="preloader">
        <div class="loader"></div>
    </div>

    <!-- Header -->
    <header>
        <div class="logo">Fit-With-AI</div>
        <div class="nav-links">
            <a href="#home">Home</a>
            <a href="#features">Features</a>
            <a href="#about">About</a>
            <a href="#gallery">Gallery</a>
            <a href="#contact">Contact</a>
        </div>
        <div class="auth-buttons">
            <button class="auth-btn signin-btn" onclick="openModal('signin')">Sign In</button>
            <button class="auth-btn signup-btn" onclick="openModal('signup')">Sign Up</button>
        </div>
    </header>

    <!-- Hero Section -->
    <section id="home" class="hero">
        <div class="hero-bg">
            <img src="https://images.unsplash.com/photo-1517836357463-d25dfeac3438?auto=format&fit=crop&w=1920&h=1080" alt="Fitness Hero Background" class="hero-image">
        </div>
        <div class="hero-content">
            <h1 class="hero-title">Discover the Power of <span class="changing-text">Innovation</span></h1>
            <p class="hero-subtitle">Join our community of creators and innovators to share your ideas and connect with like-minded individuals who are changing the world.</p>
            <div class="hero-buttons">
                <button class="hero-btn primary-btn" onclick="openModal('signup')">Get Started</button>
                <button class="hero-btn secondary-btn">Learn More</button>
            </div>
        </div>
        <div class="parallax-bg">
            <div class="parallax-circle circle-1"></div>
            <div class="parallax-circle circle-2"></div>
        </div>
    </section>

    <!-- Features Section -->
    <section id="features" class="features">
        <h2 class="section-title">Why Choose <span>Fit-With-AI</span></h2>
        <div class="features-grid">
            <div class="feature-card stagger-animate">
                <div class="feature-icon">
                    <i class="fas fa-dumbbell"></i>
                </div>
                <h3 class="feature-title">Personalized Workouts</h3>
                <p class="feature-desc">AI-generated workout plans tailored to your fitness level, goals, and available equipment that adapt as you progress.</p>
            </div>
            <div class="feature-card stagger-animate">
                <div class="feature-icon">
                    <i class="fas fa-utensils"></i>
                </div>
                <h3 class="feature-title">Nutrition Guidance</h3>
                <p class="feature-desc">Custom meal plans and nutritional advice based on your dietary preferences, restrictions, and fitness objectives.</p>
            </div>
            <div class="feature-card stagger-animate">
                <div class="feature-icon">
                    <i class="fas fa-heart-rate"></i>
                </div>
                <h3 class="feature-title">Real-time Feedback</h3>
                <p class="feature-desc">Advanced pose detection technology analyzes your form during exercises and provides instant corrections to prevent injuries.</p>
            </div>
            <div class="feature-card stagger-animate">
                <div class="feature-icon">
                    <i class="fas fa-chart-line"></i>
                </div>
                <h3 class="feature-title">Progress Tracking</h3>
                <p class="feature-desc">Comprehensive analytics that monitor your improvements, celebrate milestones, and adjust your plan for optimal results.</p>
            </div>
            <div class="feature-card stagger-animate">
                <div class="feature-icon">
                    <i class="fas fa-users"></i>
                </div>
                <h3 class="feature-title">Supportive Community</h3>
                <p class="feature-desc">Connect with fellow fitness enthusiasts, participate in challenges, and stay motivated with our thriving community.</p>
            </div>
            <div class="feature-card stagger-animate">
                <div class="feature-icon">
                    <i class="fas fa-mobile-alt"></i>
                </div>
                <h3 class="feature-title">Train Anywhere</h3>
                <p class="feature-desc">Access your workouts anywhere, anytime. Our platform works seamlessly across all devices with online and offline options.</p>
            </div>
        </div>
    </section>

    <!-- About Section -->
    <section id="about" class="about">
        <div class="about-grid">
            <div class="about-content">
                <h2 class="section-title">About <span>Fit-With-AI</span></h2>
                <p>Fit-With-AI was founded with a simple yet powerful vision: to create a platform where creative minds can connect, share, and grow together. Our team of passionate innovators is dedicated to providing you with the tools and community you need to bring your ideas to life.</p>
                <p>What started as a small project has now evolved into a global community of creators, artists, and innovators who are changing the world one idea at a time. We believe in the power of collaboration and the magic that happens when creative minds come together.</p>
                <div class="stats">
                    <div class="stat-item">
                        <div class="stat-number">2M+</div>
                        <div class="stat-label">Active Users</div>
                    </div>
                    <div class="stat-item">
                        <div class="stat-number">50M+</div>
                        <div class="stat-label">Photos Shared</div>
                    </div>
                    <div class="stat-item">
                        <div class="stat-number">120+</div>
                        <div class="stat-label">Countries</div>
                    </div>
                </div>
            </div>
            <div class="about-image-container float">
                <img src="https://images.unsplash.com/photo-1571019614242-c5c5dee9f50b?auto=format&fit=crop&w=600&h=600" alt="About Us - AI Fitness Training" class="about-image">
            </div>
        </div>
    </section>

    <!-- Gallery Section -->
    <section id="gallery" class="gallery">
        <h2 class="section-title">Our <span>Gallery</span></h2>
        <div class="gallery-grid">
            <div class="gallery-item stagger-animate">
                <img src="https://images.unsplash.com/photo-1534438327276-14e5300c3a48?auto=format&fit=crop&w=600&h=600" alt="Personal Training" class="gallery-image">
                <div class="gallery-overlay">
                    <h3 class="gallery-title">Personal Training</h3>
                    <p class="gallery-desc">Expert guidance for your fitness journey.</p>
                </div>
            </div>
            <div class="gallery-item stagger-animate">
                <img src="https://images.unsplash.com/photo-1532384748853-8f54a8f476e2?auto=format&fit=crop&w=600&h=600" alt="Nutrition Planning" class="gallery-image">
                <div class="gallery-overlay">
                    <h3 class="gallery-title">Nutrition Planning</h3>
                    <p class="gallery-desc">Customized meal plans for optimal health.</p>
                </div>
            </div>
            <div class="gallery-item stagger-animate">
                <img src="https://images.unsplash.com/photo-1518310383802-640c2de311b2?auto=format&fit=crop&w=600&h=600" alt="Strength Training" class="gallery-image">
                <div class="gallery-overlay">
                    <h3 class="gallery-title">Strength Training</h3>
                    <p class="gallery-desc">Build strength and muscle effectively.</p>
                </div>
            </div>
            <div class="gallery-item stagger-animate">
                <img src="https://images.unsplash.com/photo-1599058917765-a780eda07a3e?auto=format&fit=crop&w=600&h=600" alt="Yoga & Flexibility" class="gallery-image">
                <div class="gallery-overlay">
                    <h3 class="gallery-title">Yoga & Flexibility</h3>
                    <p class="gallery-desc">Improve flexibility and mental wellness.</p>
                </div>
            </div>
            <div class="gallery-item stagger-animate">
                <img src="https://images.unsplash.com/photo-1538805060514-97d9cc17730c?auto=format&fit=crop&w=600&h=600" alt="Cardio Workouts" class="gallery-image">
                <div class="gallery-overlay">
                    <h3 class="gallery-title">Cardio Workouts</h3>
                    <p class="gallery-desc">Effective cardio routines for endurance.</p>
                </div>
            </div>
            <div class="gallery-item stagger-animate">
                <img src="https://images.unsplash.com/photo-1579126038374-6064e9370f0f?auto=format&fit=crop&w=600&h=600" alt="Progress Tracking" class="gallery-image">
                <div class="gallery-overlay">
                    <h3 class="gallery-title">Progress Tracking</h3>
                    <p class="gallery-desc">Monitor your fitness journey.</p>
                </div>
            </div>
            <div class="gallery-item stagger-animate">
                <img src="https://images.unsplash.com/photo-1574680096145-d05b474e2155?auto=format&fit=crop&w=600&h=600" alt="Healthy Lifestyle" class="gallery-image">
                <div class="gallery-overlay">
                    <h3 class="gallery-title">Healthy Lifestyle</h3>
                    <p class="gallery-desc">Transform your life with healthy habits.</p>
                </div>
            </div>
            <div class="gallery-item stagger-animate">
                <img src="https://images.unsplash.com/photo-1576678927484-cc907957088c?auto=format&fit=crop&w=600&h=600" alt="Community Support" class="gallery-image">
                <div class="gallery-overlay">
                    <h3 class="gallery-title">Community Support</h3>
                    <p class="gallery-desc">Join our supportive fitness community.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Contact Section -->
    <section id="contact" class="about">
        <h2 class="section-title">Get In <span>Touch</span></h2>
        <div class="about-grid">
            <div class="about-content">
                <p>Have questions or want to learn more about our services? Reach out to our team today and let's start creating something amazing together.</p>
                <form class="contact-form">
                    <div class="form-group">
                        <label class="form-label">Name</label>
                        <input type="text" class="form-input" placeholder="Your Name">
                    </div>
                    <div class="form-group">
                        <label class="form-label">Email</label>
                        <input type="email" class="form-input" placeholder="Your Email">
                    </div>
                    <div class="form-group">
                        <label class="form-label">Message</label>
                        <textarea class="form-input" rows="5" placeholder="Your Message"></textarea>
                    </div>
                    <button type="submit" class="form-btn">Send Message</button>
                </form>
            </div>
            <div class="about-image-container float">
                <img src="https://images.unsplash.com/photo-1486312338219-ce68d2c6f44d?auto=format&fit=crop&w=600&h=600" alt="Contact Us - Customer Support" class="about-image">
            </div>
        </div>
    </section>

    <!-- Authentication Modal -->
    <div class="auth-modal" id="authModal">
        <div class="modal-container">
            <button class="modal-close" onclick="closeModal()">
                <i class="fas fa-times"></i>
            </button>

            <div class="modal-tabs">
                <div class="modal-tab active" id="signinTab" onclick="switchTab('signin')">Sign In</div>
                <div class="modal-tab" id="signupTab" onclick="switchTab('signup')">Sign Up</div>
            </div>

            <div class="modal-content">
                <!-- Sign In Form -->
                <form class="auth-form active" id="signinForm" action="/login" method="POST">
                    <div class="form-group">
                        <label class="form-label">Email</label>
                        <input type="email" name="email" class="form-input" placeholder="Your Email" required>
                    </div>
                    <div class="form-group">
                        <label class="form-label">Password</label>
                        <input type="password" name="password" class="form-input" placeholder="Your Password" required>
                    </div>
                    <button type="submit" class="form-btn">Sign In</button>
                    <div class="form-footer">
                        Don't have an account? <a onclick="switchTab('signup')">Sign Up</a>
                    </div>
                </form>

                <!-- Sign Up Form -->
                <form class="auth-form" id="signupForm" action="/signup" method="POST">
                    <div class="form-group">
                        <label class="form-label">Full Name</label>
                        <input type="text" name="fullName" class="form-input" placeholder="Your Full Name" required>
                    </div>
                    <div class="form-group">
                        <label class="form-label">Email</label>
                        <input type="email" name="email" class="form-input" placeholder="Your Email" required>
                    </div>
                    <div class="form-group">
                        <label class="form-label">Password</label>
                        <input type="password" name="password" class="form-input" placeholder="Create Password" required minlength="8">
                    </div>
                    <div class="form-group">
                        <label class="form-label">Confirm Password</label>
                        <input type="password" name="confirmPassword" class="form-input" placeholder="Confirm Password" required>
                    </div>
                    <button type="submit" class="form-btn">Sign Up</button>
                    <div class="form-footer">
                        Already have an account? <a onclick="switchTab('signin')">Sign In</a>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <!-- Footer -->
    <footer id="footer">
    <div class="footer-grid">
    <div class="footer-col">
    <h3>Fit-With-AI</h3>
    <p>We're a creative solutions company dedicated to bringing your ideas to life with innovative designs and cutting-edge technology.</p>
    <div class="social-links">
        <a href="#" class="social-link"><i class="fab fa-facebook-f"></i></a>
        <a href="#" class="social-link"><i class="fab fa-twitter"></i></a>
        <a href="#" class="social-link"><i class="fab fa-instagram"></i></a>
        <a href="#" class="social-link"><i class="fab fa-linkedin-in"></i></a>
    </div>
    </div>
    <div class="footer-col">
    <h3>Quick Links</h3>
    <ul>
        <li><a href="#home">Home</a></li>
        <li><a href="#features">Features</a></li>
        <li><a href="#about">About</a></li>
        <li><a href="#gallery">Gallery</a></li>
        <li><a href="#contact">Contact</a></li>
    </ul>
    </div>
    <div class="footer-col">
    <h3>Contact Info</h3>
    <ul>
        <li><i class="fas fa-map-marker-alt"></i> 123 Creative St, Design City</li>
        <li><i class="fas fa-phone"></i> +1 (555) 123-4567</li>
        <li><i class="fas fa-envelope"></i> info@Fit-With-AI.com</li>
    </ul>
    </div>
    <div class="footer-col">
    <h3>Newsletter</h3>
    <p>Subscribe to our newsletter to receive updates on our latest projects and offers.</p>
    <form class="newsletter-form">
        <div class="form-group">
            <input type="email" class="form-input" placeholder="Your Email">
        </div>
        <button type="submit" class="form-btn">Subscribe</button>
    </form>
    </div>
    </div>
    <div class="footer-bottom">
    <p>&copy; 2025 Fit-With-AI. All Rights Reserved.</p>
    </div>
    </footer>

    <script>
    // Preloader
    window.addEventListener('load', function() {
    setTimeout(function() {
    document.querySelector('.preloader').classList.add('hidden');
    }, 500);
    });

    // Changing Text Animation
    const words = ["Innovation", "Creativity", "Success", "Growth", "Excellence"];
    let currentWordIndex = 0;

    function animateText() {
    const changingText = document.querySelector('.changing-text');

    gsap.to(changingText, {
    duration: 0.5,
    opacity: 0,
    y: 20,
    onComplete: function() {
        currentWordIndex = (currentWordIndex + 1) % words.length;
        changingText.textContent = words[currentWordIndex];
        
        gsap.to(changingText, {
            duration: 0.5,
            opacity: 1,
            y: 0
        });
    }
    });
    }

    setInterval(animateText, 3000);

    // Scroll Animation
    document.addEventListener('DOMContentLoaded', function() {
    // Register ScrollTrigger plugin
    gsap.registerPlugin(ScrollTrigger);

    // Animate feature cards
    gsap.utils.toArray('.stagger-animate').forEach((element, index) => {
    gsap.from(element, {
        scrollTrigger: {
            trigger: element,
            start: "top 80%",
            toggleActions: "play none none none"
        },
        y: 50,
        opacity: 0,
        duration: 0.8,
        delay: index * 0.1
    });
    });

    // Parallax effect for background elements
    gsap.utils.toArray('.parallax-circle').forEach(circle => {
    gsap.to(circle, {
        scrollTrigger: {
            trigger: 'body',
            start: "top top",
            end: "bottom bottom",
            scrub: 1
        },
        y: circle.classList.contains('circle-1') ? 100 : -100,
        ease: "none"
    });
    });
    });

    // Authentication Modal Functions
    function openModal(tab) {
    document.getElementById('authModal').classList.add('active');
    switchTab(tab);
    }

    function closeModal() {
    document.getElementById('authModal').classList.remove('active');
    }

    function switchTab(tab) {
    // Reset tabs
    document.getElementById('signinTab').classList.remove('active');
    document.getElementById('signupTab').classList.remove('active');
    document.getElementById('signinForm').classList.remove('active');
    document.getElementById('signupForm').classList.remove('active');

    // Activate selected tab
    document.getElementById(tab + 'Tab').classList.add('active');
    document.getElementById(tab + 'Form').classList.add('active');
    }

    // Smooth scrolling for navigation
    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
        anchor.addEventListener('click', function (e) {
            e.preventDefault();

            const targetId = this.getAttribute('href');
            if(targetId === "#") return;

            const targetElement = document.querySelector(targetId);

            window.scrollTo({
                top: targetElement.offsetTop - 80,
                behavior: 'smooth'
            });
        });
    });

    // Remove the generic form prevention and only prevent for non-auth forms
    document.querySelector('.contact-form').addEventListener('submit', function(e) {
        e.preventDefault();
        alert('Contact form submission successful! (This is a demo)');
        this.reset();
    });

    document.querySelector('.newsletter-form').addEventListener('submit', function(e) {
        e.preventDefault();
        alert('Newsletter subscription successful! (This is a demo)');
        this.reset();
    });

    // Updated authentication functions
    function handleSignUp(event) {
        event.preventDefault();
        console.log('Signup form submitted');
        
        const form = document.getElementById('signupForm');
        const password = form.querySelector('input[name="password"]').value;
        const confirmPassword = form.querySelector('input[name="confirmPassword"]').value;
        const email = form.querySelector('input[name="email"]').value;
        const fullName = form.querySelector('input[name="fullName"]').value;

        console.log('Form data:', { email, fullName }); // Debug log

        if (password !== confirmPassword) {
            alert('Passwords do not match!');
            return;
        }

        const formData = {
            fullName: fullName,
            email: email,
            password: password
        };

        console.log('Sending signup request with data:', { ...formData, password: '***' });

        fetch('/signup', {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json',
            },
            body: JSON.stringify(formData)
        })
        .then(response => {
            console.log('Signup response status:', response.status);
            return response.json();
        })
        .then(data => {
            console.log('Signup response data:', data);
            if (data.success) {
                localStorage.setItem('sessionId', data.sessionId);
                window.location.href = `/CustomOnboarding?sessionId=${data.sessionId}`;
            } else {
                alert(data.error || 'Error during signup');
            }
        })
        .catch(error => {
            console.error('Signup error:', error);
            alert('Error during signup');
        });
    }

    function handleSignIn(event) {
        event.preventDefault();
        const form = document.getElementById('signinForm');
        const formData = new FormData(form);

        fetch('/login', {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json',
            },
            body: JSON.stringify(Object.fromEntries(formData)),
        })
        .then(response => response.json())
        .then(data => {
            if (data.success) {
                if (data.isOnboarded) {
                    window.location.href = '/dashboard';
                } else {
                    window.location.href = '/onboarding';
                }
            } else {
                alert(data.error || 'Invalid credentials');
            }
        })
        .catch(error => {
            console.error('Error:', error);
            alert('Error during login');
        });
    }

    // Add event listeners to forms
    document.getElementById('signinForm').addEventListener('submit', handleSignIn);
    document.getElementById('signupForm').addEventListener('submit', handleSignUp);
    </script>
</body>
</html>
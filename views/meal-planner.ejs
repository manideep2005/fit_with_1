<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AI Meal Planner - Fit-With-AI</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link rel="stylesheet" href="/css/shared-sidebar.css">
    <style>
        :root {
            --primary: #6366f1;
            --secondary: #8b5cf6;
            --accent: #f59e0b;
            --light: #f8fafc;
            --dark: #1e293b;
            --success: #10b981;
            --warning: #f59e0b;
            --danger: #ef4444;
            --gray: #64748b;
            --light-gray: #e2e8f0;
        }
        
        body {
            font-family: 'Inter', sans-serif;
            background: #ffffff;
            color: #1e293b;
            margin: 0;
            padding: 0;
            min-height: 100vh;
        }
        
        .dashboard-container {
            display: grid;
            grid-template-columns: 280px 1fr;
            min-height: 100vh;
            gap: 20px;
            padding: 20px;
        }
        
        /* Sidebar styles handled by shared-sidebar.css */
        
        .main-content {
            display: flex;
            flex-direction: column;
            gap: 25px;
        }
        
        .page-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 2rem;
        }
        
        .page-title {
            font-size: 2rem;
            font-weight: 600;
            color: var(--dark);
        }
        
        .ai-badge {
            background: var(--primary);
            color: white;
            padding: 0.75rem 1.5rem;
            border-radius: 25px;
            font-size: 0.9rem;
            font-weight: 600;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }
        
        .ai-badge i {
            animation: pulse 2s infinite;
        }
        
        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.1); }
            100% { transform: scale(1); }
        }
        
        /* Enhanced Welcome Section */
        .welcome-card {
            background: #ffffff;
            border: 1px solid var(--light-gray);
            border-radius: 12px;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
            padding: 3rem;
            text-align: center;
            margin-bottom: 2rem;
        }
        
        .welcome-header {
            margin-bottom: 3rem;
        }
        
        .ai-avatar {
            width: 100px;
            height: 100px;
            border-radius: 50%;
            background: var(--primary);
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 1.5rem;
            font-size: 2.5rem;
            color: white;
            animation: float 3s ease-in-out infinite;
        }
        
        @keyframes float {
            0%, 100% { transform: translateY(0px); }
            50% { transform: translateY(-10px); }
        }
        
        .welcome-header h2 {
            font-size: 2.5rem;
            font-weight: 700;
            color: var(--dark);
            margin-bottom: 1rem;
        }
        
        .welcome-header p {
            font-size: 1.2rem;
            color: var(--gray);
            max-width: 700px;
            margin: 0 auto;
            line-height: 1.8;
        }
        
        .features-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 2rem;
            margin-bottom: 3rem;
        }
        
        .feature-item {
            text-align: center;
            padding: 2rem;
            background: #ffffff;
            border: 1px solid var(--light-gray);
            border-radius: 12px;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
            transition: all 0.3s ease;
        }
        
        .feature-item:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 20px rgba(0,0,0,0.1);
        }
        
        .feature-icon {
            width: 80px;
            height: 80px;
            border-radius: 20px;
            background: rgba(99, 102, 241, 0.1);
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 1.5rem;
            font-size: 2rem;
            color: var(--primary);
        }
        
        .feature-item h4 {
            font-size: 1.3rem;
            font-weight: 600;
            color: var(--dark);
            margin-bottom: 1rem;
        }
        
        .feature-item p {
            color: var(--gray);
            line-height: 1.6;
        }
        
        .cta-section {
            text-align: center;
        }
        
        .btn {
            padding: 1rem 2rem;
            border: none;
            border-radius: 8px;
            font-family: 'Inter', sans-serif;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            text-decoration: none;
            font-size: 1rem;
        }
        
        .btn-primary {
            background: var(--primary);
            color: white;
        }
        
        .btn-primary:hover {
            background: var(--secondary);
        }
        
        .btn-large {
            padding: 1.25rem 2.5rem;
            font-size: 1.1rem;
            font-weight: 700;
        }
        
        .cta-note {
            margin-top: 1.5rem;
            color: var(--gray);
            font-size: 0.95rem;
        }
        
        /* Advanced Nutrition Tracker */
        .nutrition-tracker {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border-radius: 20px;
            padding: 2rem;
            margin-bottom: 2rem;
            color: white;
        }
        
        .nutrition-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1.5rem;
            margin-top: 1.5rem;
        }
        
        .nutrition-card {
            background: rgba(255, 255, 255, 0.1);
            border-radius: 15px;
            padding: 1.5rem;
            text-align: center;
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.2);
        }
        
        .nutrition-value {
            font-size: 2.5rem;
            font-weight: 700;
            margin-bottom: 0.5rem;
        }
        
        .nutrition-progress {
            width: 100%;
            height: 8px;
            background: rgba(255, 255, 255, 0.2);
            border-radius: 4px;
            margin-top: 1rem;
            overflow: hidden;
        }
        
        .nutrition-progress-fill {
            height: 100%;
            background: linear-gradient(90deg, #4CAF50, #8BC34A);
            border-radius: 4px;
            transition: width 0.8s ease;
        }
        
        /* Recipe Generator */
        .recipe-generator {
            background: #ffffff;
            border-radius: 20px;
            padding: 2rem;
            margin-bottom: 2rem;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
        }
        
        .recipe-input-group {
            display: grid;
            grid-template-columns: 1fr auto;
            gap: 1rem;
            margin-bottom: 1.5rem;
        }
        
        .recipe-input {
            padding: 1rem;
            border: 2px solid var(--light-gray);
            border-radius: 12px;
            font-size: 1rem;
            transition: all 0.3s ease;
        }
        
        .recipe-input:focus {
            outline: none;
            border-color: var(--primary);
            box-shadow: 0 0 0 3px rgba(99, 102, 241, 0.1);
        }
        
        .recipe-filters {
            display: flex;
            gap: 1rem;
            margin-bottom: 1.5rem;
            flex-wrap: wrap;
        }
        
        .filter-chip {
            padding: 0.5rem 1rem;
            background: var(--light);
            border: 2px solid var(--light-gray);
            border-radius: 25px;
            cursor: pointer;
            transition: all 0.3s ease;
            font-size: 0.9rem;
        }
        
        .filter-chip.active {
            background: var(--primary);
            color: white;
            border-color: var(--primary);
        }
        
        /* Food Scanner */
        .food-scanner {
            background: linear-gradient(135deg, #ff9a9e 0%, #fecfef 50%, #fecfef 100%);
            border-radius: 20px;
            padding: 2rem;
            margin-bottom: 2rem;
            text-align: center;
        }
        
        .scanner-button {
            background: white;
            color: var(--primary);
            border: none;
            padding: 1rem 2rem;
            border-radius: 50px;
            font-size: 1.1rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
        }
        
        .scanner-button:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(0,0,0,0.15);
        }
        
        /* Meal Planning Calendar */
        .meal-calendar {
            background: white;
            border-radius: 20px;
            padding: 2rem;
            margin-bottom: 2rem;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
        }
        
        .calendar-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 2rem;
        }
        
        .calendar-nav {
            display: flex;
            gap: 1rem;
        }
        
        .calendar-nav button {
            background: var(--primary);
            color: white;
            border: none;
            padding: 0.5rem 1rem;
            border-radius: 8px;
            cursor: pointer;
            transition: all 0.3s ease;
        }
        
        .calendar-grid {
            display: grid;
            grid-template-columns: repeat(7, 1fr);
            gap: 1rem;
        }
        
        .calendar-day {
            border: 2px solid var(--light-gray);
            border-radius: 12px;
            padding: 1rem;
            min-height: 120px;
            cursor: pointer;
            transition: all 0.3s ease;
        }
        
        .calendar-day:hover {
            border-color: var(--primary);
            box-shadow: 0 5px 15px rgba(99, 102, 241, 0.2);
        }
        
        .calendar-day.selected {
            background: var(--primary);
            color: white;
            border-color: var(--primary);
        }
        
        .day-meals {
            margin-top: 0.5rem;
        }
        
        .meal-item {
            background: rgba(255, 255, 255, 0.1);
            padding: 0.25rem 0.5rem;
            border-radius: 6px;
            font-size: 0.8rem;
            margin-bottom: 0.25rem;
        }
        
        /* Enhanced Modal Styles */
        .modal {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.7);
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 1000;
            backdrop-filter: blur(5px);
        }
        
        .modal-content {
            background: #ffffff;
            border: 1px solid var(--light-gray);
            border-radius: 12px;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
            width: 90%;
            max-width: 700px;
            max-height: 85vh;
            overflow-y: auto;
        }
        
        .modal-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 2rem 2rem 1rem;
            border-bottom: 1px solid var(--light-gray);
            background: var(--primary);
            color: white;
            border-radius: 12px 12px 0 0;
        }
        
        .modal-header h2 {
            margin: 0;
            font-weight: 700;
            font-size: 1.5rem;
        }
        
        .close {
            font-size: 1.5rem;
            cursor: pointer;
            background: rgba(255, 255, 255, 0.2);
            border: none;
            color: white;
            padding: 0.5rem;
            border-radius: 50%;
            width: 40px;
            height: 40px;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.3s ease;
        }
        
        .close:hover {
            background: rgba(255, 255, 255, 0.3);
            transform: rotate(90deg);
        }
        
        .modal-body {
            padding: 2rem;
        }
        
        .assessment-progress {
            margin-bottom: 2rem;
        }
        
        .progress-bar {
            width: 100%;
            height: 12px;
            background: var(--light-gray);
            border-radius: 6px;
            overflow: hidden;
            margin-bottom: 1rem;
        }
        
        .progress-fill {
            height: 100%;
            background: var(--gradient-success);
            border-radius: 6px;
            transition: width 0.5s ease;
            position: relative;
        }
        
        .progress-fill::after {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.3), transparent);
            animation: shimmer 2s infinite;
        }
        
        @keyframes shimmer {
            0% { transform: translateX(-100%); }
            100% { transform: translateX(100%); }
        }
        
        .progress-text {
            text-align: center;
            color: var(--gray);
            font-size: 0.9rem;
            font-weight: 500;
        }
        
        .question-container {
            margin-bottom: 2rem;
        }
        
        .question {
            margin-bottom: 2rem;
        }
        
        .question-title {
            font-size: 1.3rem;
            font-weight: 600;
            color: var(--dark);
            margin-bottom: 1.5rem;
            line-height: 1.4;
        }
        
        .question-description {
            color: var(--gray);
            margin-bottom: 1rem;
            font-size: 0.95rem;
        }
        
        .question-input {
            width: 100%;
            padding: 15px 20px;
            border: 2px solid var(--light-gray);
            border-radius: 12px;
            font-size: 1rem;
            transition: all 0.3s ease;
            background: white;
            box-sizing: border-box;
        }
        
        .question-input:focus {
            outline: none;
            border-color: var(--primary);
            box-shadow: 0 0 0 3px rgba(108, 99, 255, 0.1);
        }
        
        .number-input-container {
            position: relative;
            display: block;
            width: 100%;
        }
        
        .input-unit {
            position: absolute;
            right: 15px;
            top: 50%;
            transform: translateY(-50%);
            color: var(--gray);
            font-weight: 500;
        }
        
        .question-options {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 1rem;
        }
        
        .enhanced-options {
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        }
        
        .option-card {
            border: 2px solid var(--light-gray);
            border-radius: 12px;
            padding: 1.5rem;
            cursor: pointer;
            transition: all 0.3s ease;
            text-align: center;
            background: white;
            position: relative;
        }
        
        .option-card:hover {
            border-color: var(--primary);
            box-shadow: 0 5px 15px rgba(108, 99, 255, 0.2);
            transform: translateY(-2px);
        }
        
        .option-card.selected {
            border-color: var(--success);
            background: linear-gradient(135deg, rgba(40, 167, 69, 0.1), rgba(40, 167, 69, 0.05));
            box-shadow: 0 5px 20px rgba(40, 167, 69, 0.3);
        }
        
        .enhanced-card {
            text-align: left;
        }
        
        .option-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 0.5rem;
        }
        
        .option-description {
            color: var(--gray);
            font-size: 0.9rem;
            margin: 0;
        }
        
        .multiselect-options {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 0.75rem;
        }
        
        .enhanced-multiselect {
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        }
        
        .multiselect-option {
            display: flex;
            align-items: center;
            gap: 1rem;
            padding: 1rem;
            border: 2px solid var(--light-gray);
            border-radius: 10px;
            cursor: pointer;
            transition: all 0.3s ease;
            background: white;
        }
        
        .multiselect-option:hover {
            background: rgba(108, 99, 255, 0.05);
            border-color: var(--primary);
        }
        
        .multiselect-option.selected {
            background: linear-gradient(135deg, rgba(40, 167, 69, 0.1), rgba(40, 167, 69, 0.05));
            border-color: var(--success);
        }
        
        .checkbox-container {
            position: relative;
        }
        
        .checkbox-container input[type="checkbox"] {
            opacity: 0;
            position: absolute;
        }
        
        .checkmark {
            width: 20px;
            height: 20px;
            border: 2px solid var(--light-gray);
            border-radius: 4px;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.3s ease;
        }
        
        .multiselect-option.selected .checkmark {
            background: var(--success);
            border-color: var(--success);
        }
        
        .multiselect-option.selected .checkmark::after {
            content: '✓';
            color: white;
            font-weight: bold;
            font-size: 12px;
        }
        
        .option-text {
            font-weight: 500;
            color: var(--dark);
        }
        
        .assessment-navigation {
            display: flex;
            justify-content: space-between;
            gap: 1rem;
            margin-top: 2rem;
        }
        
        .assessment-navigation .btn {
            flex: 1;
            justify-content: center;
        }
        
        .btn-outline {
            background: white;
            color: var(--primary);
            border: 2px solid var(--primary);
        }
        
        .btn-outline:hover {
            background: var(--primary);
            color: white;
        }
        
        .btn-success {
            background: var(--gradient-success);
            color: white;
            box-shadow: 0 5px 15px rgba(40, 167, 69, 0.3);
        }
        
        .btn-success:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(40, 167, 69, 0.4);
        }
        
        /* Meal Plan Display */
        .meal-plan-section {
            background: #ffffff;
            border: 1px solid var(--light-gray);
            border-radius: 12px;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
            padding: 2rem;
            margin-bottom: 2rem;
        }
        
        .daily-summary {
            background: var(--primary);
            color: white;
            border-radius: 15px;
            padding: 2rem;
            margin-bottom: 2rem;
        }
        
        .summary-title {
            font-size: 1.5rem;
            font-weight: 700;
            margin-bottom: 1.5rem;
            text-align: center;
        }
        
        .summary-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1.5rem;
        }
        
        .summary-item {
            text-align: center;
            background: rgba(255, 255, 255, 0.1);
            padding: 1.5rem;
            border-radius: 12px;
            backdrop-filter: blur(10px);
        }
        
        .summary-value {
            font-size: 2.5rem;
            font-weight: 700;
            margin-bottom: 0.5rem;
        }
        
        .summary-label {
            font-size: 0.9rem;
            opacity: 0.9;
            font-weight: 500;
        }
        
        .summary-progress {
            width: 100%;
            height: 8px;
            background: rgba(255,255,255,0.2);
            border-radius: 4px;
            margin-top: 1rem;
            overflow: hidden;
        }
        
        .summary-progress-fill {
            height: 100%;
            background: white;
            border-radius: 4px;
            transition: width 0.8s ease;
        }
        
        /* Toast Notifications */
        .toast {
            position: fixed;
            top: 20px;
            right: 20px;
            padding: 15px 25px;
            border-radius: 12px;
            color: white;
            z-index: 2000;
            font-weight: 500;
            transform: translateX(100%);
            transition: transform 0.3s ease;
            box-shadow: 0 10px 25px rgba(0,0,0,0.2);
        }
        
        .toast.show {
            transform: translateX(0);
        }
        
        .toast-success { 
            background: var(--gradient-success);
        }
        
        .toast-error { 
            background: var(--gradient-warning);
        }
        
        .toast-info { 
            background: var(--gradient-primary);
        }
        
        /* Responsive Design */
        @media (max-width: 768px) {
            .dashboard-container {
                grid-template-columns: 1fr;
            }
            
            .sidebar {
                width: 100%;
                position: relative;
                height: auto;
                padding: 1rem;
            }
            
            .main-content {
                padding: 1rem;
            }
            
            .page-header {
                flex-direction: column;
                gap: 1rem;
                text-align: center;
            }
            
            .features-grid {
                grid-template-columns: 1fr;
            }
            
            .question-options {
                grid-template-columns: 1fr;
            }
            
            .multiselect-options {
                grid-template-columns: 1fr;
            }
            
            .summary-grid {
                grid-template-columns: repeat(2, 1fr);
            }
        }
    </style>
</head>
<body>
    <div class="dashboard-container">
        <!-- Sidebar -->
        <%- include('partials/sidebar', { currentPage: 'meal-planner' }) %>
        
        <!-- Main Content -->
        <div class="main-content">
            <div class="page-header">
                <h1 class="page-title">AI Meal Planning & Tracking</h1>
                <div style="display: flex; gap: 1rem;">
                    <div class="ai-badge">
                        <i class="fas fa-robot"></i>
                        AI-Powered Nutrition
                    </div>
                    <button class="btn btn-primary" onclick="toggleAIChat()" style="padding: 0.5rem 1rem; font-size: 0.9rem;">
                        <i class="fas fa-comments"></i> Ask AI
                    </button>
                </div>
            </div>
            
            <!-- Health Assessment Modal -->
            <div id="healthAssessmentModal" class="modal" style="display: none;">
                <div class="modal-content">
                    <div class="modal-header">
                        <h2><i class="fas fa-brain"></i> AI Health Assessment</h2>
                        <span class="close" onclick="closeHealthAssessment()">&times;</span>
                    </div>
                    <div class="modal-body">
                        <div class="assessment-progress">
                            <div class="progress-bar">
                                <div class="progress-fill" id="assessmentProgress" style="width: 0%"></div>
                            </div>
                            <div class="progress-text">
                                <span id="currentQuestion">1</span> of <span id="totalQuestions">20</span> questions
                            </div>
                        </div>
                        
                        <div class="question-container" id="questionContainer">
                            <!-- Questions will be populated here -->
                        </div>
                        
                        <div class="assessment-navigation">
                            <button class="btn btn-outline" id="prevBtn" onclick="previousQuestion()" disabled>
                                <i class="fas fa-arrow-left"></i> Previous
                            </button>
                            <button class="btn btn-primary" id="nextBtn" onclick="nextQuestion()">
                                Next <i class="fas fa-arrow-right"></i>
                            </button>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Meal Plan Display -->
            <div id="mealPlanSection" class="meal-plan-section" style="display: none;">
                <!-- Daily Summary -->
                <div class="daily-summary">
                    <h2 class="summary-title">Your Personalized Nutrition Plan</h2>
                    <div class="summary-grid">
                        <div class="summary-item">
                            <div class="summary-value" id="targetCalories">0</div>
                            <div class="summary-label">Target Calories</div>
                            <div class="summary-progress">
                                <div class="summary-progress-fill" id="caloriesProgress" style="width: 0%"></div>
                            </div>
                        </div>
                        <div class="summary-item">
                            <div class="summary-value" id="targetProtein">0g</div>
                            <div class="summary-label">Target Protein</div>
                            <div class="summary-progress">
                                <div class="summary-progress-fill" id="proteinProgress" style="width: 0%"></div>
                            </div>
                        </div>
                        <div class="summary-item">
                            <div class="summary-value" id="targetCarbs">0g</div>
                            <div class="summary-label">Target Carbs</div>
                            <div class="summary-progress">
                                <div class="summary-progress-fill" id="carbsProgress" style="width: 0%"></div>
                            </div>
                        </div>
                        <div class="summary-item">
                            <div class="summary-value" id="targetFat">0g</div>
                            <div class="summary-label">Target Fat</div>
                            <div class="summary-progress">
                                <div class="summary-progress-fill" id="fatProgress" style="width: 0%"></div>
                            </div>
                        </div>
                    </div>
                </div>
                
                <!-- Weekly Meal Plan -->
                <div id="weeklyMealPlan">
                    <!-- Meal plan will be populated here -->
                </div>
            </div>

            <!-- AI Chat Assistant -->
            <div id="aiChatSection" class="welcome-card" style="display: none;">
                <div class="card-header">
                    <h3 class="card-title"><i class="fas fa-robot"></i> AI Nutrition Assistant</h3>
                    <button class="btn btn-outline" onclick="toggleAIChat()" style="padding: 8px 16px; font-size: 0.8rem;">Close Chat</button>
                </div>
                <div class="chat-container" id="nutritionChatContainer">
                    <div class="ai-message">
                        <strong>AI Nutritionist:</strong> Hi! I'm your AI nutrition assistant. Ask me anything about meal planning, nutrition, recipes, or dietary advice!
                    </div>
                </div>
                <div class="input-group">
                    <input type="text" class="form-control" id="nutritionChatInput" placeholder="Ask about nutrition, recipes, meal planning..." onkeypress="if(event.key==='Enter') sendNutritionMessage()">
                    <button class="btn btn-primary" onclick="sendNutritionMessage()">
                        <i class="fas fa-paper-plane"></i>
                    </button>
                </div>
            </div>

            <!-- Nutrition Tracker -->
            <div class="nutrition-tracker">
                <h3 style="margin-bottom: 1rem;">📊 Today's Nutrition</h3>
                <div class="nutrition-grid">
                    <div class="nutrition-card">
                        <div class="nutrition-value" id="todayCalories">1250</div>
                        <div>Calories</div>
                        <div class="nutrition-progress">
                            <div class="nutrition-progress-fill" style="width: 62%"></div>
                        </div>
                        <small>Target: 2000</small>
                    </div>
                    <div class="nutrition-card">
                        <div class="nutrition-value" id="todayProtein">85g</div>
                        <div>Protein</div>
                        <div class="nutrition-progress">
                            <div class="nutrition-progress-fill" style="width: 57%"></div>
                        </div>
                        <small>Target: 150g</small>
                    </div>
                    <div class="nutrition-card">
                        <div class="nutrition-value" id="todayCarbs">140g</div>
                        <div>Carbs</div>
                        <div class="nutrition-progress">
                            <div class="nutrition-progress-fill" style="width: 56%"></div>
                        </div>
                        <small>Target: 250g</small>
                    </div>
                    <div class="nutrition-card">
                        <div class="nutrition-value" id="todayFat">45g</div>
                        <div>Fat</div>
                        <div class="nutrition-progress">
                            <div class="nutrition-progress-fill" style="width: 67%"></div>
                        </div>
                        <small>Target: 67g</small>
                    </div>
                </div>
                <div style="text-align: center; margin-top: 1.5rem;">
                    <button class="btn btn-primary" onclick="logFood()" style="background: rgba(255,255,255,0.2); border: 2px solid white;">
                        <i class="fas fa-plus"></i> Log Food
                    </button>
                </div>
            </div>
            
            <!-- Quick Actions -->
            <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 1.5rem; margin-bottom: 2rem;">
                <div class="recipe-generator">
                    <h3 style="color: var(--primary); margin-bottom: 1rem;">🤖 AI Recipe Generator</h3>
                    <div style="margin-bottom: 1rem;">
                        <input type="text" class="recipe-input" id="recipeInput" placeholder="Enter ingredients (e.g., chicken, rice, vegetables)" style="width: 100%; margin-bottom: 1rem;">
                        <button class="btn btn-primary" onclick="generateRecipe()" style="width: 100%;">
                            <i class="fas fa-magic"></i> Generate Recipe
                        </button>
                    </div>
                    <div id="recipeResult" style="display: none; padding: 1rem; background: var(--light); border-radius: 8px;">
                        <h4 style="color: var(--primary);">Generated Recipe</h4>
                        <p id="recipeName"></p>
                        <p id="recipeInstructions"></p>
                    </div>
                </div>
                
                <div class="food-scanner">
                    <h3 style="color: white; margin-bottom: 1rem;">📱 Food Scanner</h3>
                    <p style="color: white; opacity: 0.9; margin-bottom: 1.5rem;">Scan or search for food items</p>
                    <div style="display: flex; gap: 0.5rem; justify-content: center; flex-wrap: wrap;">
                        <button class="scanner-button" onclick="searchFood()" style="font-size: 0.9rem; padding: 0.75rem 1rem;">
                            <i class="fas fa-search"></i> Search
                        </button>
                        <button class="scanner-button" onclick="quickAdd()" style="font-size: 0.9rem; padding: 0.75rem 1rem;">
                            <i class="fas fa-plus"></i> Quick Add
                        </button>
                    </div>
                </div>
            </div>
            
            <!-- Meal Plan Section -->
            <div class="meal-calendar">
                <div class="calendar-header">
                    <h3 style="color: var(--primary); margin: 0;">📅 This Week's Meal Plan</h3>
                    <div style="display: flex; gap: 1rem;">
                        <button class="btn btn-outline" onclick="generateWeeklyPlan()">
                            <i class="fas fa-magic"></i> Generate Plan
                        </button>
                        <button class="btn btn-primary" onclick="startHealthAssessment()">
                            <i class="fas fa-clipboard-list"></i> Health Assessment
                        </button>
                    </div>
                </div>
                <div style="display: grid; grid-template-columns: repeat(7, 1fr); gap: 1rem;" id="weeklyPlan">
                    <div style="background: var(--light); padding: 1rem; border-radius: 8px; text-align: center;">
                        <h4 style="margin-bottom: 0.5rem; color: var(--primary);">Mon</h4>
                        <div style="font-size: 0.8rem; color: var(--gray);">Click to add meals</div>
                    </div>
                    <div style="background: var(--light); padding: 1rem; border-radius: 8px; text-align: center;">
                        <h4 style="margin-bottom: 0.5rem; color: var(--primary);">Tue</h4>
                        <div style="font-size: 0.8rem; color: var(--gray);">Click to add meals</div>
                    </div>
                    <div style="background: var(--light); padding: 1rem; border-radius: 8px; text-align: center;">
                        <h4 style="margin-bottom: 0.5rem; color: var(--primary);">Wed</h4>
                        <div style="font-size: 0.8rem; color: var(--gray);">Click to add meals</div>
                    </div>
                    <div style="background: var(--light); padding: 1rem; border-radius: 8px; text-align: center;">
                        <h4 style="margin-bottom: 0.5rem; color: var(--primary);">Thu</h4>
                        <div style="font-size: 0.8rem; color: var(--gray);">Click to add meals</div>
                    </div>
                    <div style="background: var(--light); padding: 1rem; border-radius: 8px; text-align: center;">
                        <h4 style="margin-bottom: 0.5rem; color: var(--primary);">Fri</h4>
                        <div style="font-size: 0.8rem; color: var(--gray);">Click to add meals</div>
                    </div>
                    <div style="background: var(--light); padding: 1rem; border-radius: 8px; text-align: center;">
                        <h4 style="margin-bottom: 0.5rem; color: var(--primary);">Sat</h4>
                        <div style="font-size: 0.8rem; color: var(--gray);">Click to add meals</div>
                    </div>
                    <div style="background: var(--light); padding: 1rem; border-radius: 8px; text-align: center;">
                        <h4 style="margin-bottom: 0.5rem; color: var(--primary);">Sun</h4>
                        <div style="font-size: 0.8rem; color: var(--gray);">Click to add meals</div>
                    </div>
                </div>
            </div>
            
            <!-- Welcome Section -->
            <div id="welcomeSection">
                <div class="welcome-card">
                    <div class="welcome-header">
                        <div class="ai-avatar">
                            <i class="fas fa-utensils"></i>
                        </div>
                        <h2>AI-Powered Meal Planning</h2>
                        <p>Get personalized meal plans, track nutrition, and discover healthy recipes tailored to your goals.</p>
                        <div style="display: flex; justify-content: center; gap: 2rem; margin: 2rem 0; flex-wrap: wrap;">
                            <div style="text-align: center;">
                                <div style="background: rgba(16, 185, 129, 0.1); color: var(--success); padding: 1rem; border-radius: 50%; width: 60px; height: 60px; display: flex; align-items: center; justify-content: center; margin: 0 auto 0.5rem; font-size: 1.5rem;"><i class="fas fa-check-circle"></i></div>
                                <span style="font-size: 0.9rem; color: var(--gray);">AI-Powered</span>
                            </div>
                            <div style="text-align: center;">
                                <div style="background: rgba(99, 102, 241, 0.1); color: var(--primary); padding: 1rem; border-radius: 50%; width: 60px; height: 60px; display: flex; align-items: center; justify-content: center; margin: 0 auto 0.5rem; font-size: 1.5rem;"><i class="fas fa-utensils"></i></div>
                                <span style="font-size: 0.9rem; color: var(--gray);">Personalized</span>
                            </div>
                            <div style="text-align: center;">
                                <div style="background: rgba(245, 158, 11, 0.1); color: var(--warning); padding: 1rem; border-radius: 50%; width: 60px; height: 60px; display: flex; align-items: center; justify-content: center; margin: 0 auto 0.5rem; font-size: 1.5rem;"><i class="fas fa-clock"></i></div>
                                <span style="font-size: 0.9rem; color: var(--gray);">5 Minutes</span>
                            </div>
                        </div>
                    </div>
                    
                    <div class="features-grid">
                        <div class="feature-item">
                            <div class="feature-icon">
                                <i class="fas fa-brain"></i>
                            </div>
                            <h4>Smart Analysis</h4>
                            <p>AI analyzes your health data, medical conditions, regional preferences, and lifestyle</p>
                        </div>
                        <div class="feature-item">
                            <div class="feature-icon">
                                <i class="fas fa-globe-asia"></i>
                            </div>
                            <h4>Regional Cuisine</h4>
                            <p>Authentic North Indian, South Indian, and regional meal plans tailored to your taste</p>
                        </div>
                        <div class="feature-item">
                            <div class="feature-icon">
                                <i class="fas fa-utensils"></i>
                            </div>
                            <h4>Personalized Meals</h4>
                            <p>Custom meal plans considering dietary restrictions, cooking time, and budget</p>
                        </div>
                        <div class="feature-item">
                            <div class="feature-icon">
                                <i class="fas fa-heartbeat"></i>
                            </div>
                            <h4>Health-Focused</h4>
                            <p>Meals designed for specific medical conditions like diabetes, hypertension, and more</p>
                        </div>
                        <div class="feature-item">
                            <div class="feature-icon">
                                <i class="fas fa-chart-line"></i>
                            </div>
                            <h4>Progress Tracking</h4>
                            <p>Real-time nutrition tracking and goal monitoring with detailed analytics</p>
                        </div>
                        <div class="feature-item">
                            <div class="feature-icon">
                                <i class="fas fa-shopping-cart"></i>
                            </div>
                            <h4>Smart Shopping</h4>
                            <p>Automated grocery lists with budget optimization and local ingredient suggestions</p>
                        </div>
                    </div>
                    
                    <div class="cta-section">
                        <div style="display: flex; gap: 1rem; justify-content: center; flex-wrap: wrap;">
                            <button class="btn btn-primary btn-large" onclick="startHealthAssessment()">
                                <i class="fas fa-magic"></i> Start Health Assessment
                            </button>
                            <button class="btn btn-outline btn-large" onclick="showQuickStart()" style="background: #ffffff; color: var(--primary); border: 2px solid var(--primary);">
                                <i class="fas fa-bolt"></i> Quick Start
                            </button>
                        </div>
                        <p class="cta-note">Takes 5-7 minutes • Completely personalized • Science-backed • Regional cuisine support</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
    
    <script>
        // Global variables
        let healthQuestions = [];
        let currentQuestionIndex = 0;
        let healthAnswers = {};
        let todayNutrition = { calories: 1250, protein: 85, carbs: 140, fat: 45 };
        let nutritionTargets = { calories: 2000, protein: 150, carbs: 250, fat: 67 };
        
        // Log food function
        function logFood() {
            const foodName = prompt('Enter food name:');
            if (!foodName) return;
            
            const calories = parseInt(prompt('Enter calories:')) || 0;
            const protein = parseInt(prompt('Enter protein (g):')) || 0;
            
            todayNutrition.calories += calories;
            todayNutrition.protein += protein;
            
            document.getElementById('todayCalories').textContent = todayNutrition.calories;
            document.getElementById('todayProtein').textContent = todayNutrition.protein + 'g';
            
            showToast(`${foodName} logged successfully!`, 'success');
        }
        
        // Generate recipe function
        function generateRecipe() {
            const ingredients = document.getElementById('recipeInput').value;
            if (!ingredients) {
                showToast('Please enter some ingredients', 'error');
                return;
            }
            
            const recipe = {
                name: 'AI Generated Recipe',
                instructions: `Cook ${ingredients} with your favorite spices and seasonings. Serve hot and enjoy!`
            };
            
            document.getElementById('recipeName').textContent = recipe.name;
            document.getElementById('recipeInstructions').textContent = recipe.instructions;
            document.getElementById('recipeResult').style.display = 'block';
            
            showToast('Recipe generated!', 'success');
        }
        
        // Search food function
        function searchFood() {
            const query = prompt('Search for food:');
            if (!query) return;
            
            const foods = {
                'apple': { calories: 95, protein: 0 },
                'chicken': { calories: 165, protein: 31 },
                'rice': { calories: 130, protein: 3 }
            };
            
            const food = foods[query.toLowerCase()];
            if (food) {
                if (confirm(`Add ${query}? (${food.calories} cal, ${food.protein}g protein)`)) {
                    todayNutrition.calories += food.calories;
                    todayNutrition.protein += food.protein;
                    document.getElementById('todayCalories').textContent = todayNutrition.calories;
                    document.getElementById('todayProtein').textContent = todayNutrition.protein + 'g';
                    showToast(`${query} added!`, 'success');
                }
            } else {
                showToast('Food not found', 'error');
            }
        }
        
        // Quick add function
        function quickAdd() {
            todayNutrition.calories += 100;
            todayNutrition.protein += 5;
            document.getElementById('todayCalories').textContent = todayNutrition.calories;
            document.getElementById('todayProtein').textContent = todayNutrition.protein + 'g';
            showToast('Quick food added!', 'success');
        }
        
        // Generate weekly plan
        function generateWeeklyPlan() {
            const days = ['Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun'];
            const meals = ['Oatmeal', 'Salad', 'Fish', 'Bowl', 'Pasta', 'Smoothie', 'Wrap'];
            
            const planDiv = document.getElementById('weeklyPlan');
            planDiv.innerHTML = '';
            
            days.forEach((day, index) => {
                const dayDiv = document.createElement('div');
                dayDiv.style.cssText = 'background: white; padding: 1rem; border-radius: 8px; box-shadow: 0 2px 8px rgba(0,0,0,0.1);';
                dayDiv.innerHTML = `
                    <h4 style="margin-bottom: 0.5rem; color: var(--primary);">${day}</h4>
                    <div style="font-size: 0.8rem; margin-bottom: 0.5rem; color: var(--success);">${meals[index]}</div>
                    <div style="font-size: 0.7rem; color: var(--gray);">~500 cal</div>
                `;
                planDiv.appendChild(dayDiv);
            });
            
            showToast('Weekly meal plan generated!', 'success');
        }
        
        // Initialize the page
        document.addEventListener('DOMContentLoaded', function() {
            console.log('Enhanced Meal Planner initialized');
        });
        
        // Start health assessment
        async function startHealthAssessment() {
            try {
                console.log('Starting enhanced health assessment...');
                showToast('Loading health assessment questions...', 'info');
                
                // Load health questions from server
                const response = await fetch('/api/meal-planner/health-questions');
                const data = await response.json();
                
                console.log('Health questions response:', data);
                
                if (data.success) {
                    healthQuestions = data.questions;
                    currentQuestionIndex = 0;
                    healthAnswers = {};
                    
                    console.log('Loaded', healthQuestions.length, 'enhanced health questions');
                    
                    // Show modal and first question
                    document.getElementById('healthAssessmentModal').style.display = 'flex';
                    document.getElementById('totalQuestions').textContent = healthQuestions.length;
                    
                    showQuestion(0);
                    showToast('Health assessment started!', 'success');
                } else {
                    console.error('Failed to load health questions:', data.error);
                    showToast('Failed to load health assessment', 'error');
                }
            } catch (error) {
                console.error('Error starting health assessment:', error);
                showToast('Error starting health assessment', 'error');
            }
        }
        
        // Show specific question with enhanced UI
        function showQuestion(index) {
            if (index >= healthQuestions.length) {
                completeAssessment();
                return;
            }
            
            const question = healthQuestions[index];
            const container = document.getElementById('questionContainer');
            
            // Update progress
            const progress = ((index + 1) / healthQuestions.length) * 100;
            document.getElementById('assessmentProgress').style.width = progress + '%';
            document.getElementById('currentQuestion').textContent = index + 1;
            
            // Generate question HTML with enhanced styling
            let questionHTML = `
                <div class="question">
                    <h3 class="question-title">${question.question}</h3>
                    ${question.description ? `<p class="question-description">${question.description}</p>` : ''}
            `;
            
            switch (question.type) {
                case 'number':
                    questionHTML += `
                        <div class="number-input-container">
                            <input type="number" 
                                   class="question-input" 
                                   id="answer-${question.id}" 
                                   min="${question.min || 0}" 
                                   max="${question.max || 1000}"
                                   value="${healthAnswers[question.id] || ''}"
                                   placeholder="Enter ${question.question.toLowerCase()}"
                                   onchange="saveNumberInput('${question.id}')">
                            ${question.unit ? `<span class="input-unit">${question.unit}</span>` : ''}
                        </div>
                    `;
                    break;
                    
                case 'select':
                    if (Array.isArray(question.options) && question.options.length > 0 && typeof question.options[0] === 'object') {
                        // Options with labels
                        questionHTML += '<div class="question-options enhanced-options">';
                        question.options.forEach(option => {
                            const isSelected = healthAnswers[question.id] === option.value;
                            questionHTML += `
                                <div class="option-card enhanced-card ${isSelected ? 'selected' : ''}" 
                                     onclick="selectOption('${question.id}', '${option.value}')">
                                    <div class="option-header">
                                        <strong>${option.label}</strong>
                                        ${isSelected ? '<i class="fas fa-check-circle"></i>' : ''}
                                    </div>
                                    ${option.description ? `<p class="option-description">${option.description}</p>` : ''}
                                </div>
                            `;
                        });
                        questionHTML += '</div>';
                    } else {
                        // Simple options
                        questionHTML += '<div class="question-options">';
                        question.options.forEach(option => {
                            const isSelected = healthAnswers[question.id] === option;
                            const displayText = formatOptionText(option);
                            questionHTML += `
                                <div class="option-card ${isSelected ? 'selected' : ''}" 
                                     onclick="selectOption('${question.id}', '${option}')">
                                    <span>${displayText}</span>
                                    ${isSelected ? '<i class="fas fa-check-circle"></i>' : ''}
                                </div>
                            `;
                        });
                        questionHTML += '</div>';
                    }
                    break;
                    
                case 'multiselect':
                    questionHTML += '<div class="multiselect-options enhanced-multiselect">';
                    question.options.forEach(option => {
                        const isSelected = healthAnswers[question.id] && healthAnswers[question.id].includes(option);
                        const displayText = formatOptionText(option);
                        questionHTML += `
                            <div class="multiselect-option ${isSelected ? 'selected' : ''}" 
                                 onclick="toggleMultiOption('${question.id}', '${option}')">
                                <div class="checkbox-container">
                                    <input type="checkbox" ${isSelected ? 'checked' : ''} readonly>
                                    <span class="checkmark"></span>
                                </div>
                                <span class="option-text">${displayText}</span>
                            </div>
                        `;
                    });
                    questionHTML += '</div>';
                    break;
            }
            
            questionHTML += '</div>';
            container.innerHTML = questionHTML;
            
            // Update navigation buttons
            document.getElementById('prevBtn').disabled = index === 0;
            const nextBtn = document.getElementById('nextBtn');
            if (index === healthQuestions.length - 1) {
                nextBtn.innerHTML = 'Generate Meal Plan <i class="fas fa-magic"></i>';
                nextBtn.classList.add('btn-success');
                nextBtn.classList.remove('btn-primary');
            } else {
                nextBtn.innerHTML = 'Next <i class="fas fa-arrow-right"></i>';
                nextBtn.classList.add('btn-primary');
                nextBtn.classList.remove('btn-success');
            }
        }
        
        // Format option text for better display
        function formatOptionText(option) {
            return option.charAt(0).toUpperCase() + 
                   option.slice(1)
                         .replace(/_/g, ' ')
                         .replace(/([a-z])([A-Z])/g, '$1 $2');
        }
        
        // Save number input value
        function saveNumberInput(questionId) {
            const input = document.getElementById(`answer-${questionId}`);
            if (input && input.value) {
                healthAnswers[questionId] = input.value;
                console.log('Saved number input:', questionId, input.value);
            }
        }
        
        // Select single option - FIXED
        function selectOption(questionId, value) {
            console.log('Selecting option:', questionId, value);
            healthAnswers[questionId] = value;
            
            // Update UI - remove selected class from all options in this question
            const questionContainer = document.getElementById('questionContainer');
            const allOptions = questionContainer.querySelectorAll('.option-card');
            allOptions.forEach(card => card.classList.remove('selected'));
            
            // Add selected class to clicked option
            const clickedElement = event.target.closest('.option-card');
            if (clickedElement) {
                clickedElement.classList.add('selected');
            }
            
            console.log('Updated answers:', healthAnswers);
        }
        
        // Toggle multi-select option - FIXED
        function toggleMultiOption(questionId, value) {
            console.log('Toggling multi-option:', questionId, value);
            
            if (!healthAnswers[questionId]) {
                healthAnswers[questionId] = [];
            }
            
            const index = healthAnswers[questionId].indexOf(value);
            if (index > -1) {
                healthAnswers[questionId].splice(index, 1);
            } else {
                healthAnswers[questionId].push(value);
            }
            
            // Update UI
            const clickedElement = event.target.closest('.multiselect-option');
            if (clickedElement) {
                clickedElement.classList.toggle('selected');
                const checkbox = clickedElement.querySelector('input[type="checkbox"]');
                if (checkbox) {
                    checkbox.checked = !checkbox.checked;
                }
            }
            
            console.log('Updated multiselect answers:', healthAnswers);
        }
        
        // Validate current answer
        function validateCurrentAnswer() {
            const currentQuestion = healthQuestions[currentQuestionIndex];
            if (!currentQuestion) return true;
            
            // Save any pending number input
            const numberInput = document.getElementById(`answer-${currentQuestion.id}`);
            if (numberInput && numberInput.value) {
                healthAnswers[currentQuestion.id] = numberInput.value;
            }
            
            // Check if answer exists for required questions
            if (currentQuestion.required) {
                const answer = healthAnswers[currentQuestion.id];
                
                if (answer === undefined || answer === null || answer === '') {
                    return false;
                }
                
                // For multiselect, check if array has items
                if (currentQuestion.type === 'multiselect') {
                    return Array.isArray(answer) && answer.length > 0;
                }
                
                return true;
            }
            
            return true;
        }
        
        // Navigation functions - FIXED
        function nextQuestion() {
            console.log('Next question clicked, current index:', currentQuestionIndex);
            console.log('Current answers before validation:', healthAnswers);
            
            // Validate current answer
            if (!validateCurrentAnswer()) {
                showToast('Please answer this question before continuing', 'error');
                return;
            }
            
            console.log('Moving to next question. Current answers:', healthAnswers);
            
            currentQuestionIndex++;
            showQuestion(currentQuestionIndex);
        }
        
        function previousQuestion() {
            if (currentQuestionIndex > 0) {
                // Save current answer before going back
                validateCurrentAnswer();
                
                currentQuestionIndex--;
                showQuestion(currentQuestionIndex);
            }
        }
        
        // Complete assessment and generate AI-powered meal plan
        async function completeAssessment() {
            try {
                // Final validation
                if (!validateCurrentAnswer()) {
                    showToast('Please answer the current question before completing', 'error');
                    return;
                }
                
                showToast('🤖 AI is analyzing your health profile and generating your personalized meal plan...', 'info');
                
                console.log('Submitting health assessment for AI processing:', healthAnswers);
                
                const response = await fetch('/api/meal-planner/health-assessment', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify({
                        answers: healthAnswers
                    })
                });
                
                const data = await response.json();
                
                if (data.success) {
                    console.log('AI meal plan generated successfully:', data);
                    
                    // Close modal
                    closeHealthAssessment();
                    
                    // Show enhanced meal plan with AI insights
                    displayMealPlan(
                        data.nutritionTargets, 
                        data.mealPlan,
                        data.aiInsights || [],
                        data.personalizedTips || [],
                        data.nutritionalAnalysis || '',
                        data.metadata || {}
                    );
                    
                    if (data.metadata?.aiModel === 'gemini-pro') {
                        showToast('🎉 Your AI-powered meal plan is ready! Generated by Gemini AI', 'success');
                    } else if (data.fallbackMode) {
                        showToast('✅ Your personalized meal plan is ready! (Enhanced mode)', 'success');
                    } else {
                        showToast('✅ Your personalized meal plan is ready!', 'success');
                    }
                } else {
                    console.error('Assessment submission failed:', data.error);
                    showToast('Failed to generate meal plan: ' + data.error, 'error');
                }
            } catch (error) {
                console.error('Error completing assessment:', error);
                showToast('Error generating meal plan. Please try again.', 'error');
            }
        }
        
        // Display generated meal plan with AI enhancements
        function displayMealPlan(nutritionTargets, mealPlan, aiInsights = [], personalizedTips = [], nutritionalAnalysis = '', metadata = {}) {
            // Hide welcome section
            document.getElementById('welcomeSection').style.display = 'none';
            
            // Show meal plan section
            const mealPlanSection = document.getElementById('mealPlanSection');
            mealPlanSection.style.display = 'block';
            
            // Update nutrition targets
            document.getElementById('targetCalories').textContent = nutritionTargets.calories;
            document.getElementById('targetProtein').textContent = nutritionTargets.protein + 'g';
            document.getElementById('targetCarbs').textContent = nutritionTargets.carbs + 'g';
            document.getElementById('targetFat').textContent = nutritionTargets.fat + 'g';
            
            // Generate AI insights section
            let aiInsightsHTML = '';
            if (metadata.aiModel === 'gemini-pro') {
                aiInsightsHTML = `
                    <div class="ai-insights-section" style="background: linear-gradient(135deg, rgba(108, 99, 255, 0.2), rgba(255, 119, 198, 0.2)); border-radius: 15px; padding: 2rem; margin-bottom: 2rem; border: 1px solid rgba(255, 255, 255, 0.2);">
                        <div style="display: flex; align-items: center; margin-bottom: 1.5rem;">
                            <div style="background: var(--gradient-primary); border-radius: 50%; width: 50px; height: 50px; display: flex; align-items: center; justify-content: center; margin-right: 1rem;">
                                <i class="fas fa-robot" style="color: white; font-size: 1.5rem;"></i>
                            </div>
                            <div>
                                <h3 style="color: white; margin: 0; font-size: 1.5rem;">AI Insights & Analysis</h3>
                                <p style="color: rgba(255, 255, 255, 0.8); margin: 0; font-size: 0.9rem;">Powered by Gemini AI</p>
                            </div>
                        </div>
                        
                        ${aiInsights.length > 0 ? `
                            <div class="insights-grid" style="display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 1rem; margin-bottom: 1.5rem;">
                                ${aiInsights.map(insight => `
                                    <div style="background: rgba(255, 255, 255, 0.1); padding: 1rem; border-radius: 10px; border-left: 4px solid var(--primary);">
                                        <p style="color: white; margin: 0; font-size: 0.9rem;">${insight}</p>
                                    </div>
                                `).join('')}
                            </div>
                        ` : ''}
                        
                        ${personalizedTips.length > 0 ? `
                            <div class="personalized-tips" style="margin-bottom: 1.5rem;">
                                <h4 style="color: white; margin-bottom: 1rem;"><i class="fas fa-lightbulb"></i> Personalized Tips</h4>
                                <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: 1rem;">
                                    ${personalizedTips.slice(0, 4).map(tip => `
                                        <div style="background: rgba(40, 167, 69, 0.2); padding: 1rem; border-radius: 10px; border-left: 4px solid var(--success);">
                                            <p style="color: white; margin: 0; font-size: 0.85rem;">${tip}</p>
                                        </div>
                                    `).join('')}
                                </div>
                            </div>
                        ` : ''}
                        
                        ${nutritionalAnalysis ? `
                            <div class="nutritional-analysis" style="background: rgba(255, 255, 255, 0.05); padding: 1.5rem; border-radius: 10px; border: 1px solid rgba(255, 255, 255, 0.1);">
                                <h4 style="color: white; margin-bottom: 1rem;"><i class="fas fa-chart-line"></i> Nutritional Analysis</h4>
                                <div style="color: rgba(255, 255, 255, 0.9); font-size: 0.9rem; line-height: 1.6;">
                                    ${nutritionalAnalysis.split('\n').slice(0, 5).map(line => line.trim()).filter(line => line).map(line => `<p style="margin-bottom: 0.5rem;">${line}</p>`).join('')}
                                </div>
                            </div>
                        ` : ''}
                    </div>
                `;
            }
            
            // Generate weekly meal plan HTML
            const weeklyPlanContainer = document.getElementById('weeklyMealPlan');
            let weeklyPlanHTML = aiInsightsHTML + '<h3 style="text-align: center; margin-bottom: 2rem; color: white;">Your 7-Day AI-Generated Meal Plan</h3>';
            
            const days = ['monday', 'tuesday', 'wednesday', 'thursday', 'friday', 'saturday', 'sunday'];
            const dayNames = ['Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday', 'Sunday'];
            
            days.forEach((day, index) => {
                const dayPlan = mealPlan[day];
                if (dayPlan) {
                    weeklyPlanHTML += `
                        <div class="day-plan" style="background: rgba(255, 255, 255, 0.1); backdrop-filter: blur(10px); border-radius: 15px; padding: 2rem; margin-bottom: 2rem; border: 1px solid rgba(255, 255, 255, 0.2);">
                            <h4 style="color: white; font-size: 1.5rem; margin-bottom: 1.5rem; text-align: center;">${dayNames[index]}</h4>
                            <div class="meals-grid" style="display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 1.5rem;">
                    `;
                    
                    Object.keys(dayPlan).forEach(mealType => {
                        const meal = dayPlan[mealType];
                        weeklyPlanHTML += `
                            <div class="meal-card" style="border: 2px solid rgba(255, 255, 255, 0.2); border-radius: 12px; padding: 1.5rem; transition: all 0.3s ease; background: rgba(255, 255, 255, 0.05); position: relative;" 
                                 onmouseover="this.style.transform='translateY(-5px)'; this.style.boxShadow='0 10px 25px rgba(108, 99, 255, 0.3)'" 
                                 onmouseout="this.style.transform='translateY(0)'; this.style.boxShadow='none'">
                                
                                ${metadata.aiModel === 'gemini-pro' ? '<div style="position: absolute; top: 10px; right: 10px; background: var(--gradient-primary); color: white; padding: 0.25rem 0.5rem; border-radius: 10px; font-size: 0.7rem; font-weight: 600;"><i class="fas fa-robot"></i> AI</div>' : ''}
                                
                                <div class="meal-header" style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 1rem;">
                                    <h5 style="color: white; font-weight: 600; margin: 0; text-transform: capitalize;">${mealType.replace('_', ' ')}</h5>
                                    <span style="background: var(--gradient-primary); color: white; padding: 0.25rem 0.75rem; border-radius: 15px; font-size: 0.8rem;">${meal.calories} cal</span>
                                </div>
                                
                                <h6 style="color: var(--primary); font-weight: 600; margin-bottom: 0.5rem;">${meal.name}</h6>
                                <p style="color: rgba(255, 255, 255, 0.8); font-size: 0.9rem; margin-bottom: 1rem;">${meal.description}</p>
                                
                                <div class="nutrition-info" style="display: grid; grid-template-columns: repeat(3, 1fr); gap: 0.5rem; margin-bottom: 1rem;">
                                    <div style="text-align: center; padding: 0.5rem; background: rgba(108, 99, 255, 0.2); border-radius: 8px;">
                                        <div style="font-weight: 600; color: white;">${meal.protein}g</div>
                                        <div style="font-size: 0.8rem; color: rgba(255, 255, 255, 0.7);">Protein</div>
                                    </div>
                                    <div style="text-align: center; padding: 0.5rem; background: rgba(40, 167, 69, 0.2); border-radius: 8px;">
                                        <div style="font-weight: 600; color: white;">${meal.carbs}g</div>
                                        <div style="font-size: 0.8rem; color: rgba(255, 255, 255, 0.7);">Carbs</div>
                                    </div>
                                    <div style="text-align: center; padding: 0.5rem; background: rgba(255, 193, 7, 0.2); border-radius: 8px;">
                                        <div style="font-weight: 600; color: white;">${meal.fat}g</div>
                                        <div style="font-size: 0.8rem; color: rgba(255, 255, 255, 0.7);">Fat</div>
                                    </div>
                                </div>
                                
                                <div class="meal-details" style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 1rem;">
                                    ${meal.cookingTime ? `<div style="color: rgba(255, 255, 255, 0.8); font-size: 0.9rem;"><i class="fas fa-clock"></i> ${meal.cookingTime} min</div>` : ''}
                                    ${meal.region ? `<div style="color: rgba(255, 255, 255, 0.8); font-size: 0.9rem;"><i class="fas fa-map-marker-alt"></i> ${formatOptionText(meal.region)}</div>` : ''}
                                </div>
                                
                                <div class="meal-actions" style="display: flex; gap: 0.5rem; margin-top: 1rem;">
                                    <button onclick="showMealDetails('${day}', '${mealType}')" style="flex: 1; background: rgba(108, 99, 255, 0.2); color: white; border: 1px solid rgba(108, 99, 255, 0.5); padding: 0.5rem; border-radius: 8px; font-size: 0.8rem; cursor: pointer; transition: all 0.3s ease;" 
                                            onmouseover="this.style.background='rgba(108, 99, 255, 0.3)'" 
                                            onmouseout="this.style.background='rgba(108, 99, 255, 0.2)'">
                                        <i class="fas fa-info-circle"></i> Details
                                    </button>
                                    <button onclick="replaceMeal('${day}', '${mealType}')" style="flex: 1; background: rgba(255, 193, 7, 0.2); color: white; border: 1px solid rgba(255, 193, 7, 0.5); padding: 0.5rem; border-radius: 8px; font-size: 0.8rem; cursor: pointer; transition: all 0.3s ease;" 
                                            onmouseover="this.style.background='rgba(255, 193, 7, 0.3)'" 
                                            onmouseout="this.style.background='rgba(255, 193, 7, 0.2)'">
                                        <i class="fas fa-sync-alt"></i> Replace
                                    </button>
                                </div>
                            </div>
                        `;
                    });
                    
                    weeklyPlanHTML += `
                            </div>
                        </div>
                    `;
                }
            });
            
            // Add action buttons
            weeklyPlanHTML += `
                <div class="meal-plan-actions" style="text-align: center; margin-top: 2rem; padding: 2rem; background: rgba(255, 255, 255, 0.05); border-radius: 15px;">
                    <h4 style="color: white; margin-bottom: 1.5rem;">Meal Plan Actions</h4>
                    <div style="display: flex; gap: 1rem; justify-content: center; flex-wrap: wrap;">
                        <button onclick="generateShoppingList()" class="btn btn-primary" style="background: var(--gradient-success);">
                            <i class="fas fa-shopping-cart"></i> Generate Shopping List
                        </button>
                        <button onclick="getMealPrepSuggestions()" class="btn btn-primary" style="background: var(--gradient-warning);">
                            <i class="fas fa-utensils"></i> Meal Prep Tips
                        </button>
                        <button onclick="getPersonalizedTips()" class="btn btn-primary">
                            <i class="fas fa-lightbulb"></i> More AI Tips
                        </button>
                        <button onclick="regenerateMealPlan()" class="btn btn-outline">
                            <i class="fas fa-redo"></i> Regenerate Plan
                        </button>
                    </div>
                </div>
            `;
            
            weeklyPlanContainer.innerHTML = weeklyPlanHTML;
            
            // Store meal plan data globally for actions
            window.currentMealPlan = mealPlan;
            window.currentNutritionTargets = nutritionTargets;
            
            // Scroll to meal plan
            mealPlanSection.scrollIntoView({ behavior: 'smooth' });
        }
        
        // Close health assessment modal
        function closeHealthAssessment() {
            document.getElementById('healthAssessmentModal').style.display = 'none';
        }
        
        // Show toast notification
        function showToast(message, type) {
            const toast = document.createElement('div');
            toast.className = `toast toast-${type}`;
            toast.innerHTML = `<i class="fas fa-${type === 'success' ? 'check' : type === 'error' ? 'exclamation' : 'info'}-circle"></i> ${message}`;
            document.body.appendChild(toast);
            
            // Trigger animation
            setTimeout(() => toast.classList.add('show'), 100);
            
            // Remove after 4 seconds
            setTimeout(() => {
                toast.classList.remove('show');
                setTimeout(() => toast.remove(), 300);
            }, 4000);
        }
        
        // NEW AI-POWERED INTERACTIVE FUNCTIONS
        
        // Show detailed meal information
        async function showMealDetails(day, mealType) {
            try {
                const meal = window.currentMealPlan[day][mealType];
                if (!meal) return;
                
                showToast('Getting detailed cooking instructions...', 'info');
                
                // Get AI cooking instructions
                const response = await fetch('/api/meal-planner/ai-cooking-instructions', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({
                        mealName: meal.name,
                        ingredients: meal.ingredients || [],
                        region: meal.region || 'mixed'
                    })
                });
                
                const data = await response.json();
                
                // Create modal for meal details
                const modal = document.createElement('div');
                modal.className = 'modal';
                modal.style.display = 'flex';
                modal.innerHTML = `
                    <div class="modal-content" style="max-width: 800px;">
                        <div class="modal-header">
                            <h2><i class="fas fa-utensils"></i> ${meal.name}</h2>
                            <span class="close" onclick="this.closest('.modal').remove()">&times;</span>
                        </div>
                        <div class="modal-body">
                            <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 2rem; margin-bottom: 2rem;">
                                <div>
                                    <h4 style="color: var(--primary); margin-bottom: 1rem;">Nutrition Info</h4>
                                    <div style="background: var(--light); padding: 1rem; border-radius: 10px;">
                                        <p><strong>Calories:</strong> ${meal.calories}</p>
                                        <p><strong>Protein:</strong> ${meal.protein}g</p>
                                        <p><strong>Carbs:</strong> ${meal.carbs}g</p>
                                        <p><strong>Fat:</strong> ${meal.fat}g</p>
                                        <p><strong>Cooking Time:</strong> ${meal.cookingTime || 30} minutes</p>
                                    </div>
                                </div>
                                <div>
                                    <h4 style="color: var(--primary); margin-bottom: 1rem;">Ingredients</h4>
                                    <div style="background: var(--light); padding: 1rem; border-radius: 10px;">
                                        ${meal.ingredients ? meal.ingredients.map(ing => `<p>• ${ing}</p>`).join('') : '<p>Ingredients list will be provided</p>'}
                                    </div>
                                </div>
                            </div>
                            <div>
                                <h4 style="color: var(--primary); margin-bottom: 1rem;"><i class="fas fa-robot"></i> AI Cooking Instructions</h4>
                                <div style="background: var(--light); padding: 1.5rem; border-radius: 10px; line-height: 1.6;">
                                    ${data.success ? data.instructions.replace(/\n/g, '<br>') : 'Detailed cooking instructions will be provided based on your preferences.'}
                                </div>
                            </div>
                        </div>
                    </div>
                `;
                
                document.body.appendChild(modal);
                
            } catch (error) {
                console.error('Error showing meal details:', error);
                showToast('Error loading meal details', 'error');
            }
        }
        
        // Replace meal with AI suggestions
        async function replaceMeal(day, mealType) {
            try {
                const currentMeal = window.currentMealPlan[day][mealType];
                if (!currentMeal) return;
                
                showToast('AI is finding alternative meals for you...', 'info');
                
                const response = await fetch('/api/meal-planner/ai-meal-replacement', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({
                        currentMeal: { ...currentMeal, mealType },
                        reason: 'user_preference',
                        preferences: {
                            region: currentMeal.region || 'mixed',
                            dietaryRestrictions: []
                        }
                    })
                });
                
                const data = await response.json();
                
                if (data.success && data.replacements.length > 0) {
                    // Show replacement options modal
                    const modal = document.createElement('div');
                    modal.className = 'modal';
                    modal.style.display = 'flex';
                    modal.innerHTML = `
                        <div class="modal-content" style="max-width: 900px;">
                            <div class="modal-header">
                                <h2><i class="fas fa-sync-alt"></i> Replace ${currentMeal.name}</h2>
                                <span class="close" onclick="this.closest('.modal').remove()">&times;</span>
                            </div>
                            <div class="modal-body">
                                <p style="margin-bottom: 2rem; color: var(--gray);">Choose an alternative meal generated by AI:</p>
                                <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 1.5rem;">
                                    ${data.replacements.slice(0, 4).map((replacement, index) => `
                                        <div class="replacement-option" style="border: 2px solid var(--light-gray); border-radius: 12px; padding: 1.5rem; cursor: pointer; transition: all 0.3s ease;" 
                                             onclick="selectReplacement('${day}', '${mealType}', ${index})" 
                                             onmouseover="this.style.borderColor='var(--primary)'; this.style.transform='translateY(-2px)'" 
                                             onmouseout="this.style.borderColor='var(--light-gray)'; this.style.transform='translateY(0)'">
                                            <h4 style="color: var(--primary); margin-bottom: 0.5rem;">${replacement.name}</h4>
                                            <p style="color: var(--gray); font-size: 0.9rem; margin-bottom: 1rem;">${replacement.description || 'Delicious and nutritious meal option'}</p>
                                            <div style="display: flex; justify-content: space-between; font-size: 0.8rem; color: var(--gray);">
                                                <span><i class="fas fa-fire"></i> ${replacement.calories} cal</span>
                                                <span><i class="fas fa-clock"></i> ${replacement.cookingTime || 30} min</span>
                                            </div>
                                        </div>
                                    `).join('')}
                                </div>
                            </div>
                        </div>
                    `;
                    
                    document.body.appendChild(modal);
                    
                    // Store replacements for selection
                    window.currentReplacements = data.replacements;
                    window.replacementTarget = { day, mealType };
                    
                } else {
                    showToast('No alternative meals found. Try again later.', 'error');
                }
                
            } catch (error) {
                console.error('Error replacing meal:', error);
                showToast('Error finding replacement meals', 'error');
            }
        }
        
        // Select replacement meal
        function selectReplacement(day, mealType, replacementIndex) {
            try {
                const replacement = window.currentReplacements[replacementIndex];
                if (!replacement) return;
                
                // Update the meal plan
                window.currentMealPlan[day][mealType] = {
                    ...replacement,
                    calories: replacement.calories || 400,
                    protein: replacement.protein || 20,
                    carbs: replacement.carbs || 45,
                    fat: replacement.fat || 15
                };
                
                // Close modal
                document.querySelector('.modal').remove();
                
                // Refresh display
                displayMealPlan(
                    window.currentNutritionTargets,
                    window.currentMealPlan,
                    [],
                    [],
                    '',
                    { aiModel: 'gemini-pro' }
                );
                
                showToast(`Meal replaced with ${replacement.name}!`, 'success');
                
            } catch (error) {
                console.error('Error selecting replacement:', error);
                showToast('Error updating meal', 'error');
            }
        }
        
        // Generate shopping list
        async function generateShoppingList() {
            try {
                if (!window.currentMealPlan) {
                    showToast('No meal plan available', 'error');
                    return;
                }
                
                showToast('Generating your shopping list...', 'info');
                
                const response = await fetch('/api/meal-planner/shopping-list', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ mealPlan: window.currentMealPlan })
                });
                
                const data = await response.json();
                
                if (data.success) {
                    showShoppingListModal(data.shoppingList);
                } else {
                    showToast('Error generating shopping list', 'error');
                }
                
            } catch (error) {
                console.error('Error generating shopping list:', error);
                showToast('Error generating shopping list', 'error');
            }
        }
        
        // Show shopping list modal
        function showShoppingListModal(shoppingList) {
            const modal = document.createElement('div');
            modal.className = 'modal';
            modal.style.display = 'flex';
            
            let categoriesHTML = '';
            Object.keys(shoppingList.categories || {}).forEach(category => {
                const items = shoppingList.categories[category];
                if (items.length > 0) {
                    categoriesHTML += `
                        <div style="margin-bottom: 2rem;">
                            <h4 style="color: var(--primary); margin-bottom: 1rem; text-transform: capitalize;">
                                <i class="fas fa-${getCategoryIcon(category)}"></i> ${category.replace('_', ' ')}
                            </h4>
                            <div style="background: var(--light); padding: 1rem; border-radius: 10px;">
                                ${items.map(item => `
                                    <div style="display: flex; justify-content: space-between; padding: 0.5rem 0; border-bottom: 1px solid var(--light-gray);">
                                        <span>${item.name}</span>
                                        <span style="color: var(--gray);">${item.quantity} ${item.unit}</span>
                                    </div>
                                `).join('')}
                            </div>
                        </div>
                    `;
                }
            });
            
            modal.innerHTML = `
                <div class="modal-content" style="max-width: 700px;">
                    <div class="modal-header">
                        <h2><i class="fas fa-shopping-cart"></i> Shopping List</h2>
                        <span class="close" onclick="this.closest('.modal').remove()">&times;</span>
                    </div>
                    <div class="modal-body">
                        <div style="text-align: center; margin-bottom: 2rem; padding: 1rem; background: var(--gradient-primary); color: white; border-radius: 10px;">
                            <h3 style="margin: 0;">Total Items: ${shoppingList.totalItems || 0}</h3>
                            <p style="margin: 0.5rem 0 0 0;">Estimated Cost: ₹${shoppingList.estimatedTotalCost || 0}</p>
                        </div>
                        ${categoriesHTML}
                        <div style="text-align: center; margin-top: 2rem;">
                            <button onclick="downloadShoppingList()" class="btn btn-primary">
                                <i class="fas fa-download"></i> Download List
                            </button>
                        </div>
                    </div>
                </div>
            `;
            
            document.body.appendChild(modal);
            window.currentShoppingList = shoppingList;
        }
        
        // Get category icon
        function getCategoryIcon(category) {
            const icons = {
                vegetables: 'carrot',
                fruits: 'apple-alt',
                grains: 'bread-slice',
                proteins: 'drumstick-bite',
                dairy: 'cheese',
                spices: 'pepper-hot',
                oils: 'tint',
                nuts: 'seedling'
            };
            return icons[category] || 'shopping-basket';
        }
        
        // Get meal prep suggestions
        async function getMealPrepSuggestions() {
            try {
                if (!window.currentMealPlan) {
                    showToast('No meal plan available', 'error');
                    return;
                }
                
                showToast('Getting meal prep suggestions...', 'info');
                
                const response = await fetch('/api/meal-planner/meal-prep-suggestions', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ 
                        mealPlan: window.currentMealPlan,
                        cookingTimePreference: 'moderate'
                    })
                });
                
                const data = await response.json();
                
                if (data.success) {
                    showMealPrepModal(data.suggestions);
                } else {
                    showToast('Error getting meal prep suggestions', 'error');
                }
                
            } catch (error) {
                console.error('Error getting meal prep suggestions:', error);
                showToast('Error getting meal prep suggestions', 'error');
            }
        }
        
        // Show meal prep modal
        function showMealPrepModal(suggestions) {
            const modal = document.createElement('div');
            modal.className = 'modal';
            modal.style.display = 'flex';
            modal.innerHTML = `
                <div class="modal-content" style="max-width: 800px;">
                    <div class="modal-header">
                        <h2><i class="fas fa-utensils"></i> Meal Prep Suggestions</h2>
                        <span class="close" onclick="this.closest('.modal').remove()">&times;</span>
                    </div>
                    <div class="modal-body">
                        ${suggestions.length > 0 ? suggestions.map(suggestion => `
                            <div style="background: var(--light); padding: 1.5rem; border-radius: 10px; margin-bottom: 1.5rem; border-left: 4px solid var(--primary);">
                                <h4 style="color: var(--primary); margin-bottom: 0.5rem;">${suggestion.title}</h4>
                                <p style="color: var(--gray); margin-bottom: 1rem;">${suggestion.description}</p>
                                <div style="display: flex; justify-content: space-between; font-size: 0.9rem; color: var(--success);">
                                    <span><i class="fas fa-clock"></i> Saves: ${suggestion.timesSaved}</span>
                                    <span><i class="fas fa-utensils"></i> Type: ${suggestion.type.replace('_', ' ')}</span>
                                </div>
                            </div>
                        `).join('') : '<p style="text-align: center; color: var(--gray);">No meal prep suggestions available at the moment.</p>'}
                    </div>
                </div>
            `;
            
            document.body.appendChild(modal);
        }
        
        // Get more personalized tips
        async function getPersonalizedTips() {
            try {
                showToast('Getting personalized nutrition tips...', 'info');
                
                const response = await fetch('/api/meal-planner/ai-personalized-tips', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' }
                });
                
                const data = await response.json();
                
                if (data.success) {
                    showPersonalizedTipsModal(data.tips);
                } else {
                    showToast('Error getting personalized tips', 'error');
                }
                
            } catch (error) {
                console.error('Error getting personalized tips:', error);
                showToast('Error getting personalized tips', 'error');
            }
        }
        
        // Show personalized tips modal
        function showPersonalizedTipsModal(tips) {
            const modal = document.createElement('div');
            modal.className = 'modal';
            modal.style.display = 'flex';
            modal.innerHTML = `
                <div class="modal-content" style="max-width: 700px;">
                    <div class="modal-header">
                        <h2><i class="fas fa-lightbulb"></i> Personalized Nutrition Tips</h2>
                        <span class="close" onclick="this.closest('.modal').remove()">&times;</span>
                    </div>
                    <div class="modal-body">
                        <div style="background: var(--gradient-primary); color: white; padding: 1rem; border-radius: 10px; margin-bottom: 2rem; text-align: center;">
                            <p style="margin: 0;"><i class="fas fa-robot"></i> AI-Generated Tips Just for You</p>
                        </div>
                        ${tips.length > 0 ? tips.map((tip, index) => `
                            <div style="background: var(--light); padding: 1.5rem; border-radius: 10px; margin-bottom: 1rem; border-left: 4px solid var(--success);">
                                <div style="display: flex; align-items: center; margin-bottom: 0.5rem;">
                                    <span style="background: var(--success); color: white; border-radius: 50%; width: 25px; height: 25px; display: flex; align-items: center; justify-content: center; font-size: 0.8rem; margin-right: 1rem;">${index + 1}</span>
                                    <h5 style="margin: 0; color: var(--primary);">Tip ${index + 1}</h5>
                                </div>
                                <p style="margin: 0; color: var(--dark); line-height: 1.6;">${tip}</p>
                            </div>
                        `).join('') : '<p style="text-align: center; color: var(--gray);">No personalized tips available at the moment.</p>'}
                    </div>
                </div>
            `;
            
            document.body.appendChild(modal);
        }
        
        // Regenerate meal plan
        function regenerateMealPlan() {
            if (confirm('Are you sure you want to generate a new meal plan? This will replace your current plan.')) {
                startHealthAssessment();
            }
        }
        
        // Download shopping list
        function downloadShoppingList() {
            if (!window.currentShoppingList) return;
            
            let content = 'SHOPPING LIST\n\n';
            content += `Total Items: ${window.currentShoppingList.totalItems}\n`;
            content += `Estimated Cost: ₹${window.currentShoppingList.estimatedTotalCost}\n\n`;
            
            Object.keys(window.currentShoppingList.categories || {}).forEach(category => {
                const items = window.currentShoppingList.categories[category];
                if (items.length > 0) {
                    content += `${category.toUpperCase().replace('_', ' ')}:\n`;
                    items.forEach(item => {
                        content += `- ${item.name} (${item.quantity} ${item.unit})\n`;
                    });
                    content += '\n';
                }
            });
            
            const blob = new Blob([content], { type: 'text/plain' });
            const url = window.URL.createObjectURL(blob);
            const a = document.createElement('a');
            a.href = url;
            a.download = 'shopping-list.txt';
            a.click();
            window.URL.revokeObjectURL(url);
            
            showToast('Shopping list downloaded!', 'success');
        }
        
        // Quick start function
        function showQuickStart() {
            const modal = document.createElement('div');
            modal.className = 'modal';
            modal.style.display = 'flex';
            modal.innerHTML = `
                <div class="modal-content" style="max-width: 600px;">
                    <div class="modal-header">
                        <h2><i class="fas fa-bolt"></i> Quick Start Options</h2>
                        <span class="close" onclick="this.closest('.modal').remove()">&times;</span>
                    </div>
                    <div class="modal-body">
                        <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: 1.5rem;">
                            <div class="quick-option" onclick="generateSamplePlan('weight-loss')" style="border: 2px solid var(--light-gray); border-radius: 12px; padding: 1.5rem; cursor: pointer; text-align: center; transition: all 0.3s ease;" onmouseover="this.style.borderColor='var(--primary)'; this.style.transform='translateY(-2px)'" onmouseout="this.style.borderColor='var(--light-gray)'; this.style.transform='translateY(0)'">
                                <div style="background: rgba(239, 68, 68, 0.1); color: var(--danger); padding: 1rem; border-radius: 50%; width: 60px; height: 60px; display: flex; align-items: center; justify-content: center; margin: 0 auto 1rem; font-size: 1.5rem;"><i class="fas fa-weight"></i></div>
                                <h4 style="color: var(--dark); margin-bottom: 0.5rem;">Weight Loss Plan</h4>
                                <p style="color: var(--gray); font-size: 0.9rem; margin: 0;">1500-1800 calories, high protein</p>
                            </div>
                            <div class="quick-option" onclick="generateSamplePlan('muscle-gain')" style="border: 2px solid var(--light-gray); border-radius: 12px; padding: 1.5rem; cursor: pointer; text-align: center; transition: all 0.3s ease;" onmouseover="this.style.borderColor='var(--primary)'; this.style.transform='translateY(-2px)'" onmouseout="this.style.borderColor='var(--light-gray)'; this.style.transform='translateY(0)'">
                                <div style="background: rgba(16, 185, 129, 0.1); color: var(--success); padding: 1rem; border-radius: 50%; width: 60px; height: 60px; display: flex; align-items: center; justify-content: center; margin: 0 auto 1rem; font-size: 1.5rem;"><i class="fas fa-dumbbell"></i></div>
                                <h4 style="color: var(--dark); margin-bottom: 0.5rem;">Muscle Gain Plan</h4>
                                <p style="color: var(--gray); font-size: 0.9rem; margin: 0;">2200-2800 calories, balanced macros</p>
                            </div>
                            <div class="quick-option" onclick="generateSamplePlan('maintenance')" style="border: 2px solid var(--light-gray); border-radius: 12px; padding: 1.5rem; cursor: pointer; text-align: center; transition: all 0.3s ease;" onmouseover="this.style.borderColor='var(--primary)'; this.style.transform='translateY(-2px)'" onmouseout="this.style.borderColor='var(--light-gray)'; this.style.transform='translateY(0)'">
                                <div style="background: rgba(99, 102, 241, 0.1); color: var(--primary); padding: 1rem; border-radius: 50%; width: 60px; height: 60px; display: flex; align-items: center; justify-content: center; margin: 0 auto 1rem; font-size: 1.5rem;"><i class="fas fa-balance-scale"></i></div>
                                <h4 style="color: var(--dark); margin-bottom: 0.5rem;">Maintenance Plan</h4>
                                <p style="color: var(--gray); font-size: 0.9rem; margin: 0;">1800-2200 calories, balanced nutrition</p>
                            </div>
                            <div class="quick-option" onclick="generateSamplePlan('healthy')" style="border: 2px solid var(--light-gray); border-radius: 12px; padding: 1.5rem; cursor: pointer; text-align: center; transition: all 0.3s ease;" onmouseover="this.style.borderColor='var(--primary)'; this.style.transform='translateY(-2px)'" onmouseout="this.style.borderColor='var(--light-gray)'; this.style.transform='translateY(0)'">
                                <div style="background: rgba(245, 158, 11, 0.1); color: var(--warning); padding: 1rem; border-radius: 50%; width: 60px; height: 60px; display: flex; align-items: center; justify-content: center; margin: 0 auto 1rem; font-size: 1.5rem;"><i class="fas fa-heart"></i></div>
                                <h4 style="color: var(--dark); margin-bottom: 0.5rem;">Healthy Living</h4>
                                <p style="color: var(--gray); font-size: 0.9rem; margin: 0;">Focus on whole foods & nutrients</p>
                            </div>
                        </div>
                        <div style="text-align: center; margin-top: 2rem; padding-top: 1.5rem; border-top: 1px solid var(--light-gray);">
                            <p style="color: var(--gray); font-size: 0.9rem;">Quick plans are generated instantly. For personalized plans, use the full health assessment.</p>
                        </div>
                    </div>
                </div>
            `;
            document.body.appendChild(modal);
        }
        
        // Generate sample meal plan
        async function generateSamplePlan(type) {
            try {
                showToast('Generating your sample meal plan...', 'info');
                
                // Close modal
                document.querySelector('.modal').remove();
                
                const response = await fetch('/api/meal-planner/quick-plan', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ planType: type })
                });
                
                const data = await response.json();
                
                if (data.success) {
                    displayMealPlan(
                        data.nutritionTargets,
                        data.mealPlan,
                        [],
                        [],
                        '',
                        { quickPlan: true, planType: type }
                    );
                    showToast('Sample meal plan generated!', 'success');
                } else {
                    showToast('Failed to generate sample plan', 'error');
                }
            } catch (error) {
                console.error('Error generating sample plan:', error);
                showToast('Error generating sample plan', 'error');
            }
        }
        
        // Gemini AI Integration for Accurate Nutrition
        const GEMINI_CONFIG = {
            apiKey: 'AIzaSyD45XTxnGs-dmlN_0LtMLyCy0CbfDqfcG4',
            model: 'gemini-pro',
            accurateNutrition: true
        };
        
        // Get accurate nutrition data using Gemini AI
        async function getAccurateNutritionData(foodItem, quantity = 100) {
            try {
                const response = await fetch('/api/meal-planner/gemini-nutrition', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ 
                        food: foodItem, 
                        quantity: quantity,
                        unit: 'grams',
                        useGemini: true
                    })
                });
                
                const data = await response.json();
                if (data.success && data.nutrition) {
                    // Add accuracy indicator
                    data.nutrition.accuracy = 'high';
                    data.nutrition.source = 'Gemini AI + USDA';
                    return data.nutrition;
                }
                return null;
            } catch (error) {
                console.error('Gemini nutrition lookup error:', error);
                return null;
            }
        }
        
        // Calculate accurate meal calories
        async function calculateMealNutrition(ingredients) {
            const nutritionPromises = ingredients.map(ing => 
                getAccurateNutritionData(ing.name, ing.quantity)
            );
            
            const nutritionData = await Promise.all(nutritionPromises);
            
            return nutritionData.reduce((total, item) => {
                if (item) {
                    total.calories += item.calories || 0;
                    total.protein += item.protein || 0;
                    total.carbs += item.carbs || 0;
                    total.fat += item.fat || 0;
                    total.fiber += item.fiber || 0;
                    total.sugar += item.sugar || 0;
                    total.sodium += item.sodium || 0;
                }
                return total;
            }, { calories: 0, protein: 0, carbs: 0, fat: 0, fiber: 0, sugar: 0, sodium: 0 });
        }
        
        // AI Chat Functions
        function toggleAIChat() {
            const chatSection = document.getElementById('aiChatSection');
            const welcomeSection = document.getElementById('welcomeSection');
            const mealPlanSection = document.getElementById('mealPlanSection');
            
            if (chatSection.style.display === 'none') {
                chatSection.style.display = 'block';
                welcomeSection.style.display = 'none';
                if (mealPlanSection) mealPlanSection.style.display = 'none';
            } else {
                chatSection.style.display = 'none';
                if (window.currentMealPlan) {
                    mealPlanSection.style.display = 'block';
                } else {
                    welcomeSection.style.display = 'block';
                }
            }
        }
        
        async function sendNutritionMessage() {
            const input = document.getElementById('nutritionChatInput');
            const message = input.value.trim();
            if (!message) return;
            
            const chatContainer = document.getElementById('nutritionChatContainer');
            
            // Add user message
            chatContainer.innerHTML += `
                <div class="user-message">
                    <strong>You:</strong> ${message}
                </div>
            `;
            
            input.value = '';
            chatContainer.scrollTop = chatContainer.scrollHeight;
            
            // Add typing indicator
            chatContainer.innerHTML += `
                <div class="ai-message" id="typingIndicator">
                    <strong>AI Nutritionist:</strong> <i class="fas fa-spinner fa-spin"></i> Thinking...
                </div>
            `;
            chatContainer.scrollTop = chatContainer.scrollHeight;
            
            try {
                const response = await fetch('/api/meal-planner/gemini-chat', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ 
                        message: message,
                        context: {
                            hasMealPlan: !!window.currentMealPlan,
                            userAnswers: healthAnswers || {},
                            requireAccurateData: true,
                            useGeminiAI: true,
                            nutritionDatabase: 'USDA'
                        }
                    })
                });
                
                const data = await response.json();
                
                // Remove typing indicator
                document.getElementById('typingIndicator').remove();
                
                if (data.success) {
                    chatContainer.innerHTML += `
                        <div class="ai-message">
                            <strong>AI Nutritionist:</strong> ${data.response}
                            ${data.suggestions ? `
                                <div style="margin-top: 1rem; padding: 1rem; background: rgba(99, 102, 241, 0.05); border-radius: 8px;">
                                    <strong>Quick Actions:</strong>
                                    <div style="display: flex; gap: 0.5rem; margin-top: 0.5rem; flex-wrap: wrap;">
                                        ${data.suggestions.map(suggestion => `
                                            <button onclick="handleAISuggestion('${suggestion.action}', '${suggestion.data}')" 
                                                    style="background: var(--primary); color: white; border: none; padding: 0.25rem 0.75rem; border-radius: 15px; font-size: 0.8rem; cursor: pointer;">
                                                ${suggestion.label}
                                            </button>
                                        `).join('')}
                                    </div>
                                </div>
                            ` : ''}
                        </div>
                    `;
                } else {
                    chatContainer.innerHTML += `
                        <div class="ai-message">
                            <strong>AI Nutritionist:</strong> I'm sorry, I'm having trouble processing your request right now. Please try again.
                        </div>
                    `;
                }
            } catch (error) {
                document.getElementById('typingIndicator').remove();
                chatContainer.innerHTML += `
                    <div class="ai-message">
                        <strong>AI Nutritionist:</strong> I'm experiencing technical difficulties. Please try again later.
                    </div>
                `;
            }
            
            chatContainer.scrollTop = chatContainer.scrollHeight;
        }
        
        // Handle AI suggestions
        async function handleAISuggestion(action, data) {
            switch(action) {
                case 'generate_recipe':
                    await getAIRecipe(data);
                    break;
                case 'nutrition_analysis':
                    await getNutritionAnalysis(data);
                    break;
                case 'meal_substitute':
                    await suggestMealSubstitute(data);
                    break;
                case 'shopping_list':
                    await generateShoppingList();
                    break;
                default:
                    showToast('Feature coming soon!', 'info');
            }
        }
        
        // Gemini AI Recipe Generation with Accurate Nutrition
        async function getAIRecipe(dishName) {
            try {
                showToast('Gemini AI is creating an accurate recipe...', 'info');
                
                const response = await fetch('/api/meal-planner/gemini-recipe', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ 
                        dishName: dishName,
                        dietaryRestrictions: healthAnswers?.dietary_restrictions || [],
                        accurateNutrition: true
                    })
                });
                
                const data = await response.json();
                
                if (data.success && data.recipe) {
                    // Validate nutrition accuracy
                    const validatedRecipe = await validateRecipeNutrition(data.recipe);
                    showRecipeModal(validatedRecipe);
                    showToast('✅ Recipe generated with verified nutrition data!', 'success');
                } else {
                    showToast('Failed to generate recipe', 'error');
                }
            } catch (error) {
                console.error('Gemini recipe error:', error);
                showToast('Error generating recipe', 'error');
            }
        }
        
        // Validate recipe nutrition accuracy
        async function validateRecipeNutrition(recipe) {
            if (!recipe.ingredients) return recipe;
            
            const calculatedNutrition = await calculateMealNutrition(recipe.ingredients);
            
            // Compare with provided nutrition and flag discrepancies
            const accuracy = {
                calories: Math.abs(recipe.nutrition.calories - calculatedNutrition.calories) < 50,
                protein: Math.abs(recipe.nutrition.protein - calculatedNutrition.protein) < 5,
                verified: true
            };
            
            return { ...recipe, calculatedNutrition, accuracy };
        }
        
        // Show recipe modal
        function showRecipeModal(recipe) {
            const modal = document.createElement('div');
            modal.className = 'modal';
            modal.style.display = 'flex';
            modal.innerHTML = `
                <div class="modal-content" style="max-width: 800px;">
                    <div class="modal-header">
                        <h2><i class="fas fa-utensils"></i> ${recipe.name}</h2>
                        <span class="close" onclick="this.closest('.modal').remove()">&times;</span>
                    </div>
                    <div class="modal-body">
                        <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 2rem; margin-bottom: 2rem;">
                            <div>
                                <h4 style="color: var(--primary); margin-bottom: 1rem;">Ingredients</h4>
                                <div style="background: var(--light); padding: 1rem; border-radius: 10px;">
                                    ${recipe.ingredients.map(ing => `<p>• ${ing}</p>`).join('')}
                                </div>
                            </div>
                            <div>
                                <h4 style="color: var(--primary); margin-bottom: 1rem;">Nutrition Info</h4>
                                <div style="background: var(--light); padding: 1rem; border-radius: 10px;">
                                    <p><strong>Calories:</strong> ${recipe.nutrition.calories}</p>
                                    <p><strong>Protein:</strong> ${recipe.nutrition.protein}g</p>
                                    <p><strong>Carbs:</strong> ${recipe.nutrition.carbs}g</p>
                                    <p><strong>Fat:</strong> ${recipe.nutrition.fat}g</p>
                                    <p><strong>Prep Time:</strong> ${recipe.prepTime} min</p>
                                </div>
                            </div>
                        </div>
                        <div>
                            <h4 style="color: var(--primary); margin-bottom: 1rem;">Instructions</h4>
                            <div style="background: var(--light); padding: 1.5rem; border-radius: 10px; line-height: 1.6;">
                                ${recipe.instructions.split('\n').map((step, i) => `<p><strong>Step ${i+1}:</strong> ${step}</p>`).join('')}
                            </div>
                        </div>
                    </div>
                </div>
            `;
            document.body.appendChild(modal);
        }
        
        // AI Nutrition Analysis
        async function getNutritionAnalysis(food) {
            try {
                showToast('Analyzing nutrition...', 'info');
                
                const response = await fetch('/api/meal-planner/ai-nutrition-analysis', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ food: food })
                });
                
                const data = await response.json();
                
                if (data.success) {
                    const chatContainer = document.getElementById('nutritionChatContainer');
                    chatContainer.innerHTML += `
                        <div class="ai-message">
                            <strong>AI Nutritionist:</strong> Here's the nutrition analysis for ${food}:
                            <div style="margin-top: 1rem; padding: 1rem; background: rgba(16, 185, 129, 0.1); border-radius: 8px;">
                                ${data.analysis}
                            </div>
                        </div>
                    `;
                    chatContainer.scrollTop = chatContainer.scrollHeight;
                } else {
                    showToast('Failed to analyze nutrition', 'error');
                }
            } catch (error) {
                showToast('Error analyzing nutrition', 'error');
            }
        }
        
        // Set user profile for accurate calculations
        function setUserProfile() {
            const age = parseInt(prompt('Enter your age:')) || 25;
            const gender = prompt('Enter your gender (male/female):') || 'male';
            const weight = parseFloat(prompt('Enter your weight (kg):')) || 70;
            const height = parseFloat(prompt('Enter your height (cm):')) || 170;
            const activityLevel = prompt('Activity level (sedentary/light/moderate/active/very_active):') || 'moderate';
            const goal = prompt('Goal (weight_loss/muscle_gain/maintenance):') || 'maintenance';
            
            userProfile = { age, gender, weight, height, activityLevel, goal };
            
            initializeNutritionTargets();
            
            // Update target displays
            document.getElementById('calorieTarget').textContent = `Target: ${nutritionTargets.calories}`;
            document.getElementById('proteinTarget').textContent = `Target: ${nutritionTargets.protein}g`;
            document.getElementById('carbTarget').textContent = `Target: ${nutritionTargets.carbs}g`;
            document.getElementById('fatTarget').textContent = `Target: ${nutritionTargets.fat}g`;
            
            updateNutritionDisplay();
            showToast('Profile updated! Targets calculated using BMR/TDEE formulas', 'success');
        }
        
        // Show algorithm information
        function showAlgorithmInfo() {
            alert(`Algorithm Details:\n\n1. BMR Calculation: Mifflin-St Jeor Equation\n   Male: 10×weight + 6.25×height - 5×age + 5\n   Female: 10×weight + 6.25×height - 5×age - 161\n\n2. TDEE: BMR × Activity Multiplier\n   Sedentary: 1.2, Light: 1.375, Moderate: 1.55\n\n3. Food Database: Hash Map for O(1) lookups\n\n4. Fuzzy Search: Levenshtein Distance Algorithm\n\n5. Meal Optimization: Priority Queue with scoring\n\n6. Recipe Generation: Combinatorial optimization`);
        }
        
        // Logout confirmation
        function confirmLogout() {
            return confirm('Are you sure you want to logout?');
        }
    </script>
    
    <script src="/js/voice-assistant-siri.js"></script>
    <script src="/js/smart-voice-assistant.js"></script>
</body>
</html>